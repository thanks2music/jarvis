# ClaudeCode ドキュメント同期レポート

> 調査日: 2026-08-16
> 前回調査: 2026-08-12 (v2.1.227 時点)
> 正ソース: 英語版 (en)
> 本日時点の最新 CHANGELOG: **v2.1.233** (BOSS のローカル環境も v2.1.233)

## サマリ

- 新機能: 11 件 (新規ファイル 0 件 / 既存追記 11 件)
- 更新必要: 12 件 (うち**事実誤り 3 件**)
- ドリフト是正: 0 件 (CLAUDE.md / README.md の目次ドリフトは検出されず)
- 情報記録のみ: 4 件 (deferred 解消 3 件 + スコープ外 1 件)
- 合計: **27 項目**

前回同期からわずか 4 日だが、**v2.1.228 / 229 / 231 / 232 / 233 の 5 バージョン**がリリースされている
(v2.1.230 は欠番)。うち **auto mode 既定化の施行・subagent fork mode の既定 ON・Task/Todo ツールの
モデル別無効化**の 3 件は BOSS のセッション挙動に直接影響する。

> **whats-new の状況**: 週次インデックスは Week 32 (08-03〜07) が最新のままで、`2026-w33` は 404 (未公開)。
> したがって今回も **CHANGELOG + 公式 docs ページが唯一の一次情報**である。

---

## 更新必要 — 事実誤り (最優先)

### 1. config-files.md / sub-agents.md — `dialogExpiry` の既定値が誤り

- 該当箇所: `docs/config-files.md:324` / `docs/sub-agents.md:208`
- 現状: 「既定 `"10m"`」と 2 箇所に記載
- 公式最新: **既定は `"5m"`**。settings ページに「**Default**: `"5m"`」と明記。cross-session-messaging ページも
  「The deadline **defaults to five minutes**」「the same `dialogExpiry` deadline the dialog uses, **five minutes by default**」と 2 箇所で 5 分と記載
- **旧キーワード**: `既定 ` + `"10m"` (`dialogExpiry` 行の `"10m"`)
- **波及ファイル**: `docs/config-files.md` / `docs/sub-agents.md` (grep 実施済み。他ファイルにヒットなし)
- 対応案: 2 箇所を `"5m"` に訂正。あわせて公式が挙げる受理値 (`"60s"` / `"5m"` / `"10m"` / `"never"`) と
  「user / managed / `--settings` からのみ読まれる」制約を追記する
- 出典: https://code.claude.com/docs/en/settings / https://code.claude.com/docs/en/cross-session-messaging
- 備考: 調査 SubAgent は「公式内で表記揺れの疑い」と報告したが、**JARVIS が settings ページ本文を直接取得して
  `"5m"` で確定**した。表記揺れではなく、ローカル doc 側の単純な誤りである

### 2. config-files.md / best-practices.md / harness.md — auto mode 既定化が「予告」のまま + **バージョンゲートが完全に欠落**

- 該当箇所: `docs/config-files.md:176-192` (🔔 節) / `docs/best-practices.md:637-642` / `docs/harness.md:527-529`
- 現状: 「**2026-08-14 から** …既定 permission mode が Manual から auto に変わる」という**未来形の告知ベース**
- 公式最新: permission-modes ページが**現在形の断定**に変更済み
  「On Pro, Max, and Team plans, the built-in starting mode is auto mode.」
  さらにローカルに**一切書かれていない**重要仕様が 3 点ある:
  1. **バージョンゲート**: 「The built-in `auto` default requires Claude Code **v2.1.228 or later** on macOS,
     Linux, and WSL, and **v2.1.233 or later** on native Windows. On earlier versions, the built-in default is Manual.」
  2. **「どのモードでセッションが始まるか」の 3 段階決定順序**: CLI flag → `permissions.defaultMode` → built-in default
  3. **built-in default が `auto` にならない除外条件 6 行** (先頭一致で適用):
     `disableAutoMode: "disable"` / feature-flag fetching オフまたは**インストール・アップグレード直後の初回セッション** /
     **`claude -p`・Agent SDK** / Bedrock・Google Cloud's Agent Platform・Foundry・Claude Platform on AWS・apps gateway /
     Enterprise プランまたは Console API key
- **旧キーワード**: `2026-08-14 から` / `2026-08-14 以降に開始するセッション` / `becomes the default`
- **波及ファイル**: `docs/config-files.md` / `docs/best-practices.md` / `docs/harness.md` (grep 実施済み)
- 対応案: 3 ファイルとも「予告」→「施行済み」に書き換え、上記 1〜3 を追記。
  **`harness.md §4.13` は特に重要**で、現状のままだと「`-p` バッチ実行も既定 auto」と読めてしまうが、
  実際には **`-p` / Agent SDK は除外条件に該当し Manual 既定のまま**である。ハーネス設計の前提が変わる
- 出典: https://code.claude.com/docs/en/permission-modes
- 注意: best-practices.md は重要文書のため慎重な確認をお願いする
- **BOSS への実務インパクト**: BOSS は Max プラン + v2.1.233 のため**条件を満たしており、対話セッションは既に auto 既定**。
  一方 `-p` を使うバッチ実行は従来どおり Manual 既定である

### 3. sub-agents.md — fork subagent は「環境変数必須」ではなくなった (v2.1.232)

- 該当箇所: `docs/sub-agents.md:92`
- 現状: 「Forked subagents: 会話コンテキスト全体を継承する fork 実行（**環境変数 `CLAUDE_CODE_FORK_SUBAGENT=1`**）」
  → 有効化に env var が必要と読める
- 公式最新: 「Claude Code turns fork mode on **by default in interactive sessions** and leaves it off by default
  in non-interactive mode with `-p` and in the Agent SDK. The interactive default requires Claude Code
  **v2.1.232 or later**.」env var は**上書き専用に降格** (`1` = `-p`/SDK でも ON、`0` = 全セッション OFF)。
  非 teammate の agent spawn も対話セッションでは既定 background 実行になった
- **旧キーワード**: `環境変数 ` + `CLAUDE_CODE_FORK_SUBAGENT=1`
- **波及ファイル**: `docs/sub-agents.md` のみ (grep 実施済み)
- 対応案: 既定 ON + バージョン条件 + `-p`/SDK は既定 OFF + env var の役割降格を記述
- 出典: https://code.claude.com/docs/en/sub-agents / https://code.claude.com/docs/en/env-vars

---

## 更新必要 — 新仕様の欠落

### 4. sub-agents.md / config-files.md — Task / Todo ツールがモデル別に既定無効化 (v2.1.233、破壊的)

- 該当箇所: `docs/sub-agents.md:94` (background subagent が保持する組み込みツール一覧に `TodoWrite` が含まれる)
- 公式最新: tools-reference に新節 "Task tool availability" が追加され、
  「In Claude Code v2.1.233 and later, the following tools aren't available on **Opus 4.8, Sonnet 5, Fable 5,
  Mythos 5, or later versions of those families** unless you opt in: `TodoWrite`, `TaskCreate`, `TaskGet`,
  `TaskUpdate`, `TaskList`.」
  復帰方法は `CLAUDE_CODE_ENABLE_TODO_TOOLS=1` / `--allowedTools TaskCreate` / `--tools` / SDK の `allowedTools`。
  **Opus 4.7 では従来どおり提供**され、background session と Claude Code on the web では全モデルで提供される
- **旧キーワード**: `TodoWrite` (sub-agents.md:94 のツール一覧内)
- **波及ファイル**: `docs/sub-agents.md` (`TodoWrite` 記載) / `docs/hooks.md:152` (`TaskCreated` hook の説明は
  ツール提供有無と独立のため修正不要だが、注記の追加余地あり)
- 対応案: sub-agents.md のツール一覧に「モデル世代により `TodoWrite` は既定で提供されない」注記を追加し、
  config-files.md の env var 記述に `CLAUDE_CODE_ENABLE_TODO_TOOLS` を追加する
- 出典: https://code.claude.com/docs/en/tools-reference#task-tool-availability
- ⚠️ **公式記述と実機観測の差異 (要注意)**: 本セッション (Opus 5 / v2.1.233 / BOSS の設定に
  `CLAUDE_CODE_ENABLE_TODO_TOOLS` の記載なし) では、**`TodoWrite` は実際に提供されていない**一方、
  **`TaskCreate` / `TaskGet` / `TaskUpdate` / `TaskList` は deferred tool として提供されている**。
  公式は 5 つすべてを対象と書いているため、記述と実機挙動が一致しない。ドキュメント化する際は
  公式記述を正としつつ「実機では Task 系のみ提供されるケースを確認 (2026-08-16)」と観測を併記するのが安全と考える

### 5. config-files.md — permission mode の開始モード決定ロジックと Shift+Tab サイクルの変更

- 該当箇所: `docs/config-files.md:160` (Shift+Tab サイクル) / `:173` (project/local の `"auto"` 無視)
- 公式最新:
  - Shift+Tab の挙動: 「From `auto`, the first press switches to `default`, and the cycle then runs
    `default` → `acceptEdits` → `plan`」
  - project/local settings の `"auto"` は**無視されるだけでなく、その場合 `~/.claude/settings.json` の
    `defaultMode` にフォールバックせず built-in default が使われる** (ローカルは「無視される」までしか書いていない)
- **旧キーワード**: `Shift+Tab` + `サイクル切替` / `project / local settings に書いても無視される`
- **波及ファイル**: `docs/config-files.md` / `docs/best-practices.md:289` (Shift+Tab 言及)
- 出典: https://code.claude.com/docs/en/permission-modes
- 備考: この挙動はメモリ `reference_permission_mode_auto_scope` の内容とも関係するため、反映後にメモリ更新を提案する

### 6. sub-agents.md / config-files.md — cross-session messaging の 4 つの拡張 (v2.1.229 / 232)

- 該当箇所: `docs/sub-agents.md:185-212` (cross-session 節) / `docs/config-files.md:324` 付近
- 公式最新 (JARVIS が cross-session-messaging ページ全文を直接取得して確認):
  1. **`@` メンションによるセッション指名 (v2.1.232〜)**: 「type `@` followed by the first letters of the
     session's name and pick the session from the typeahead」。同名の live session が複数ある場合は送信前に確認を求める
  2. **セッション名のユニーク化**: 既存の live session と同名で起動 / `/rename` すると variant 名が割り当てられる。
     `/list-agents` は各ローカルセッションの cwd を表示して同名判別を可能にする
  3. **bare name 配送**: 「One session answers to the name: Claude Code delivers the message on the name alone」
     (従来は ref による曖昧性解消が必要)
  4. **`ListAgents` のラベル (v2.1.229)**: クラウドセッションは `cloud`、Remote Control 接続が切れたセッションは
     `offline` と表示
  5. **`/config` に 2 行追加 (v2.1.232)**: 「Dialog expiry」「Messages from your other sessions」。いずれも
     user settings に書き込み、managed / `--settings` がキーを設定中は非表示。
     **`/config crossSessionInbound=value` のショートハンドは拒否される**
- **旧キーワード**: `ref` (sub-agents.md の SendMessage 節) / `v2.1.225 以降は Remote Control 経由で`
- **波及ファイル**: `docs/sub-agents.md` / `docs/config-files.md` / `docs/slash-commands.md` (`/list-agents` 記載)
- 出典: https://code.claude.com/docs/en/cross-session-messaging

### 7. plugins.md — marketplace の `command` source が未記載 (v2.1.229)

- 該当箇所: `docs/plugins.md:565-570` (`source` タイプの一覧。現状は ローカルパス / `github` / `git-subdir` / `npm` の 4 種)
- 公式最新: 第 5 の source として **`command`** が追加された。ローカルコマンドが plugin ディレクトリを stdout に
  出力し、**セッションごとに再解決されるため再起動不要**。フィールドは `command` (ASCII 500 字以内) /
  `timeout` (既定 60・最大 600) / `mode` (`"link"` で in-place 利用)。
  **version は常に出力のハッシュを含むため、version 固定ピンが効かない唯一の source** である。
  v2.1.120-228 では install に失敗し、それ以前は marketplace 全体のロードに失敗する
- **旧キーワード**: `source` は以下のタイプが指定できる (plugins.md の 4 種リスト)
- **波及ファイル**: `docs/plugins.md` のみ
- 出典: https://code.claude.com/docs/en/plugin-marketplaces#command-sources
- 備考: avengers リポの symlink 運用と相性が良い可能性があるため、BOSS の実運用で検討価値がある

### 8. plugins.md — GitLab の一級サポート (v2.1.232 / 233)

- 公式最新:
  - plugin marketplace が **bare な `gitlab.com` repo URL (ネストした subgroup 含む) を `github.com` と同様に clone** する
    (※ plugin-marketplaces docs 側は未反映 = doc lag。CHANGELOG が一次情報)
  - GitLab トークンの secret redaction 追加 (`glrt-` `gloas-` `glptt-` `glagent-` `glimt-` `glsoat-` `glcbt-`
    `glft-` `glffct-`。`glpat-` / `gldt-` は全体 redaction)。`glab` CLI の config store を `gh` 同等に保護
  - **GitLab MR URL を `--worktree` と `claude agents` view が対応** (MR は `!N` 表示) (v2.1.233)
- **旧キーワード**: `GitHub リポジトリ` (plugins.md の source 説明) / `GitHub org 単位`
- **波及ファイル**: `docs/plugins.md` / `docs/sub-agents.md` (`claude agents` view)
- 出典: CHANGELOG v2.1.232 / v2.1.233

### 9. plugins.md — `/plugin install` が marketplace を先に refresh する (v2.1.232)

- 該当箇所: `docs/plugins.md:451` 付近 (「`/plugin marketplace update` だけでは個別プラグインのバージョンは上がらない」)
- 公式最新: 現状の記述自体は依然正しいが、**`/plugin install plugin@marketplace` が事前に marketplace を refresh する**
  ようになったため、新規公開プラグインは手動 update なしでインストールできる
- **旧キーワード**: `marketplace update` + `だけでは`
- **波及ファイル**: `docs/plugins.md`
- 出典: CHANGELOG v2.1.232
- 備考: メモリ `reference_avengers_plugin_install_flow` (marketplace update → plugin install → 再起動が必須)
  の前半が緩和された可能性がある。反映後にメモリ更新を提案する

### 10. config-files.md — セキュリティ・サンドボックス関連の変更 4 件 (v2.1.228〜233)

- 公式最新:
  - **ネストした git リポジトリが親の trust を継承しなくなった** (v2.1.232)。各リポジトリで個別に trust 確認が要る
  - **`sandbox.ripgrep` は user / managed / `--settings` からのみ読まれる** ようになった (project から上書き不可)。
    `bwrapPath` / `socatPath` / `ripgrep` の server-managed 上書きは managed 承認必須
  - **Windows NT `\??\` device prefix が UNC path 検証をバイパスする脆弱性** (NTLM credential leak 経路) を修正 (v2.1.233)
  - PowerShell の `$PSDefaultParameterValues` 上書きによる権限バイパス / Linux filesystem sandbox の
    protected-path バイパス / cross-session messaging socket dir (`/tmp`) の事前配置 symlink 拒否 (v2.1.232)
  - **v2.1.232 の「Bash 入力リダイレクト `< file` の権限チェック」と Cygwin symlink 変更は v2.1.233 で revert 済み**
    (後日より狭い版で再投入予定)
- 該当箇所: `docs/config-files.md:68` (workspace trust) / `:376` (サンドボックスのセキュリティ修正注記) /
  `docs/best-practices.md:1145` (「v2.1.222〜v2.1.225 のセキュリティ修正」節)
- **旧キーワード**: `v2.1.222〜v2.1.225 のセキュリティ修正` (節見出しの版数レンジ)
- **波及ファイル**: `docs/config-files.md` / `docs/best-practices.md`
- 対応案: best-practices.md の節見出しを `v2.1.222〜v2.1.233` に延長し、上記を追記
- 出典: CHANGELOG v2.1.228 / v2.1.232 / v2.1.233

### 11. config-files.md — 新規環境変数・設定キー 6 件

| キー | 導入 | 公式 docs 掲載 |
|---|---|---|
| `CLAUDE_CODE_ENABLE_TODO_TOOLS` | v2.1.233 | ✅ env-vars 掲載済 |
| `CLAUDE_CODE_FORK_SUBAGENT` | v2.1.232 | ✅ env-vars 掲載済 |
| `CLAUDE_CODE_WORKFLOW_PREFIX_STAGGER_MS` | v2.1.229 | ✅ 掲載済 (既定 5000ms。fan-out 時に同一 prefix の兄弟を待たせ prompt cache を再利用。`DISABLE_PROMPT_CACHING` 時は待たない) |
| `CLAUDE_CODE_TOOL_MEMORY_LIMIT` | v2.1.233 | ❌ 未掲載 (Linux memory cgroup で Bash tool を制限。opt-in) |
| `CLAUDE_CODE_WEBFETCH_CACHE_TTL_MS` | v2.1.233 | ❌ 未掲載 (既定 15 分) |
| `additionalMarketplaces` / `allowedMarketplaces` | v2.1.232 | ❌ 未掲載 (`extraKnownMarketplaces` / `strictKnownMarketplaces` の friendlier alias) |

- **波及ファイル**: `docs/config-files.md` (設定キー表・env var 表) / `docs/plugins.md` (marketplace alias) /
  `docs/harness.md` (`CLAUDE_CODE_WORKFLOW_PREFIX_STAGGER_MS` は fan-out 設計に効くため §4 に追記価値あり)
- 注記: JARVIS が settings ページを直接取得して**上表の「❌ 未掲載」4 件が実際に公式 settings ページに存在しないこと**を確認済み。
  これらは **CHANGELOG を一次情報として採用**し、doc-lag である旨を明記して記述するのが妥当と考える
- 出典: CHANGELOG v2.1.229 / v2.1.232 / v2.1.233 / https://code.claude.com/docs/en/settings

### 12. slash-commands.md — コマンド挙動の変更 4 件

- `/code-review` が **high / xhigh / max effort でも background agent 実行**になった (v2.1.232)。
  現状 `docs/slash-commands.md:653` は「v2.1.218 以降は background subagent として実行」とだけ書いており、
  当時は一部 effort が対象外だった経緯が反映されていない
  - **旧キーワード**: `v2.1.218 以降は background subagent として実行`
- **`/advisor` が Fable 5 を再び受理するようになった** (v2.1.232、Fable アクセスのある org 向け。
  `/model fable` で usage-credits 同意)。現状 `docs/slash-commands.md:193` の
  「⚠️ **Fable 5 は advisor として提供されておらず `/advisor fable` は拒否される**（公式明記）」は**失効した記述**
  - **旧キーワード**: `/advisor fable` + `拒否される`
- **`/commit-push-pr` が危険フラグ (`--force` `--amend` `--no-verify` 等) を自動承認しなくなった** (v2.1.229)。
  現状 `docs/slash-commands.md:436` の「configured push remote への push を auto-allow」は部分的に上書きされる
  - **旧キーワード**: `push を auto-allow`
- bundled skill alias (`/checkup` `/review`) が `-p` モード・plugin/MCP ロード時に "Unknown command" になる不具合を修正 (v2.1.233)
- **波及ファイル**: `docs/slash-commands.md` のみ (grep 実施済み)
- 出典: CHANGELOG v2.1.229 / v2.1.232 / v2.1.233

### 13. best-practices.md — Write tool のルール変更 (v2.1.228)

- 公式最新: **新世代モデルは未 Read のファイルを Write で上書きできる**ようになった (Edit と同ルール)。
  旧モデルは従来どおり Read 必須
- **旧キーワード**: (現状 docs に該当記述が無いことを grep で確認済み。新規追記扱い)
- **波及ファイル**: `docs/best-practices.md` (ツール挙動節)
- 出典: CHANGELOG v2.1.228

### 14. session-history.md / claude-desktop.md — Remote Control と self-hosted runner の変更

- **Remote Control** (v2.1.228 / 229 / 232): `claude remote-control --continue` が docs に明記 /
  archive 済みセッションも `--continue`・`--session-id` で unarchive 可能 (v2.1.228) /
  bridge が cloud セッションの transcript・credential を継承するバグ修正 / 約 30 分の再接続維持 /
  「他デバイスに奪われた / 他アプリで終了 / 削除された」を区別表示
- **self-hosted runner** (v2.1.229 / 233): server 供給 hook 対応 / Windows は `--base-dir` 必須 (既定 checkout dir なし) /
  セッション開始の高速化 (branch 作成時に working tree を書き換えない) / `managed-mcp.json` 配備時の起動失敗を
  warning スキップに変更
- **波及ファイル**: `docs/session-history.md` (Remote Control の記載なしのため新規追記) / `docs/claude-desktop.md:66` 付近
- 出典: CHANGELOG v2.1.228 / v2.1.229 / v2.1.232 / v2.1.233 / https://code.claude.com/docs/en/remote-control

### 15. memory.md — Cowork セッションの `@import` 挙動 (v2.1.232)

- 公式最新: **Cowork セッションは user スコープ memory ファイルの外部 `@`-import をインライン展開しなくなった**
- 該当箇所: `docs/memory.md` (grep の結果 Cowork の記述が **0 件**)
- **波及ファイル**: `docs/memory.md`
- 出典: CHANGELOG v2.1.232
- 備考: BOSS の `~/.claude/CLAUDE.md` は `@~/.claude/work-style.md` 等の外部 import を多用しているため、
  Cowork 利用時に挙動が変わる点は実務上意味がある

### 16. plugins.md — `claude plugin validate` の対象拡張 (v2.1.233)

- 該当箇所: `docs/plugins.md:388` (「`claude plugin validate` の対象拡張 (v2.1.196)」)
- 公式最新: v2.1.233 で **bare な `.claude/skills` ディレクトリも検査対象**になり、frontmatter のパースに
  失敗した SKILL.md を報告するようになった
- **旧キーワード**: `claude plugin validate` + `の対象拡張`
- **波及ファイル**: `docs/plugins.md`
- 出典: CHANGELOG v2.1.233

### 17. config-files.md — v2.1.222〜225 の版数表を v2.1.233 まで延長

- 該当箇所: `docs/config-files.md:331-343` (「v2.1.222〜v2.1.225 の設定・挙動変更（2026-08-12 追記）」表)
- 対応案: 表の見出しを `v2.1.222〜v2.1.233` に変更し、項目 10・11 の内容を行として追加する
- **旧キーワード**: `v2.1.222〜v2.1.225 の設定・挙動変更`
- **波及ファイル**: `docs/config-files.md` / `docs/sub-agents.md:164` (「v2.1.222〜v2.1.225 の追加変更 (2026-08)」節)
- 出典: CHANGELOG v2.1.228-233

### 18. その他の小規模変更 (まとめて 1 項目)

- MCP OAuth を `127.0.0.1` 化 (v2.1.229) + pre-registered client (Slack) の redirect URI 不一致修正 (v2.1.231)
- MCP v2 接続の subscriptions/listen stream 再オープン暴走を修正 (**"MCP v2" という呼称の存在が示唆される**)
- `/feedback`・`/bug` が応答中でも即座に開くようになった
- agent panel で完了 subagent を即座に非表示 + `/tasks` フッタヒント
- print mode に `[claude-code:unrecognized_model]` の stderr 診断を追加 (`modelOverrides` で解消)
- カスタム subagent 作成を勧める起動 tip と `/powerup` の該当ナッジを削除 (v2.1.232)
- Claude Desktop・VS Code 配下で Notification hook が permission prompt に発火しないバグを修正
- **波及ファイル**: `docs/mcp-setup.md` / `docs/sub-agents.md` / `docs/slash-commands.md` / `docs/hooks.md`
- 出典: CHANGELOG v2.1.228-233

---

## 新機能 — 公式ブログ由来の追記

### 19. best-practices.md — 2026-08-14 ブログ「Maximizing the value of your Claude Code sessions」(追記推奨・分量大)

- 追記先: `docs/best-practices.md` のコスト / コンテキスト管理節
- 要点 (公式のトークン効率ガイダンス):
  - **モデルと effort はセッション開始前に決める**。会話途中の `/model` `/effort` 変更は **prompt cache を破棄**し、
    トークン費用が跳ね上がる
  - タスク間で `/clear`。中断前は cache が生きているうちに `/compact`
    (**subscription は 1 時間、API キーは 5 分でキャッシュ失効**)
  - **不要ターンの除去は `/compact` より `/rewind`**。rewind は末尾を切るだけで手前の cache が生き、コストがゼロ
  - ファイルはパスを書くより **`@` メンション** (Read 呼び出しを 1 回節約)
  - 新セッションでまず `/context` を実行し、プリロード内容 (CLAUDE.md / MCP tool 定義) を確認して削る
  - 大出力を残したくないタスク (ログ解析等) は subagent へ回す
- 出典: https://claude.com/blog/maximizing-the-value-of-your-claude-code-sessions
- 注意: best-practices.md は重要文書のため慎重な確認をお願いする
- ⚠️ **BOSS の運用ルールとの衝突**: この公式指針は、`~/.claude/CLAUDE.md` → `work-style.md` /
  メモリ `feedback_effort_operation_policy` にある「**JARVIS は仕様設計・難デバッグ等で `/effort xhigh` 切替を提案する**」
  と正面から衝突する。公式は「途中変更は cache を壊す」ため**会話冒頭で固定せよ**と言っている。
  → 詳細は項目 27 を参照

### 20. harness.md — `CLAUDE_CODE_WORKFLOW_PREFIX_STAGGER_MS` を fan-out 設計に反映

- 追記先: `docs/harness.md` §4 (Dynamic workflows 節)
- 要点: fan-out 時に同一 prefix の兄弟エージェントを既定 5000ms 待たせ、prompt cache を再利用する仕組みが
  既定で入っている。`DISABLE_PROMPT_CACHING` 時は待たない。**大量ファンアウトの体感レイテンシの説明になる**
- 出典: https://code.claude.com/docs/en/env-vars / CHANGELOG v2.1.229

---

## 情報記録のみ (docs 変更は小 or 不要)

### 21. deferred #23 解消 — `/tui` の導入バージョンが判明

- **v2.1.110** で追加。「Added `/tui` command and `tui` setting — run `/tui fullscreen` to switch to
  flicker-free rendering in the same conversation」。**ただし commands ページには依然未掲載**
  (v2.1.228 / 232 でも修正エントリがあり現役)
- 対応案: `docs/slash-commands.md:10` の「裏取り待ち」注記に導入バージョンを追記し、
  「CHANGELOG のみが一次情報」と明記する
- 出典: CHANGELOG v2.1.110 / https://code.claude.com/docs/en/commands

### 22. deferred #22 解消 — Channels の導入バージョンが判明

- **v2.1.80** で `--channels` (research preview) として追加。関連: v2.1.81 permission relay /
  v2.1.84 `allowedChannelPlugins` / v2.1.128 で console (API key) 認証対応 + `channelsEnabled` 必須化。
  docs 側にはバージョン記載なし
- 対応案: `docs/mcp-setup.md` の channels 節に導入バージョンを追記
- 出典: CHANGELOG v2.1.80 / v2.1.128 / https://code.claude.com/docs/en/channels

### 23. deferred #18 解消 — Sonnet 5 の tokenizer 世代が判明

- **新 tokenizer を使用。同一テキストで Sonnet 4.6 比 約 +30% トークン**。1M context の実収容テキスト量は減り、
  `max_tokens` の見直しが必要。per-token 単価低下分は相殺される。
  ※ Opus 4.7 / Opus 5 / Fable 5 と**同一世代の tokenizer かは公式に記載なし = 引き続き未確認**
- 対応案: `docs/model-comparison.md` の tokenizer 節に追記
- 出典: https://platform.claude.com/docs/en/about-claude/models/whats-new-sonnet-5

### 24. deferred #15 — `/workshop` は引き続き未確認

- commands ページ・全 docs ページ・CHANGELOG 全 5,534 行のいずれにも "workshop" の記載なし。
  **公式に存在の痕跡が見つからない**。deferred のまま据え置きを提案する

### 25. モデル関連 — 変化なし (model-comparison.md は今回変更不要)

- 2026-08-10 以降、**新しいモデル世代の発表なし**。現行は Fable 5 / Opus 5 / Sonnet 5 / Haiku 4.5 の 4 本
- 価格・context・knowledge cutoff・alias 解決先・effort 既定 (effort 対応モデルは既定 `high`、Opus 4.7 のみ `xhigh`)
  すべて既知の値と一致
- deprecation の最新エントリは 2026-06-05 (Opus 4.1、2026-08-05 retire 済) のままで新規告知なし。
  最も近い期日は `claude-sonnet-4-5-20250929` の tentative retirement **2026-09-29**
- fast mode 対象も Opus 5 / Opus 4.8 のみで変化なし
- 出典: https://platform.claude.com/docs/en/about-claude/models/overview /
  https://platform.claude.com/docs/en/about-claude/model-deprecations / https://code.claude.com/docs/en/fast-mode

### 26. スコープ外 — Enterprise 向け機能 (反映見送りを提案)

- 2026-08-11「Compliance API coverage extends to Claude Cowork and Claude Code」: Enterprise 向け beta。
  CLI と desktop app のセッション transcript を既存 Compliance Access Key で取得可。
  **Claude Code on the web・Claude Platform 経由・Bedrock/Vertex/Foundry は対象外**
- 2026-08-13 JetBrains の Fable 5 導入事例 / Claude Tag / Cowork 改名 / 2026-08-14「How Claude's text watermark works」
- いずれも本リポ docs のスコープ外と判断し、状態ファイルの `deferred_items` に記録するのみを提案する
- 出典: https://claude.com/blog/compliance-api-cowork-and-claude-code

---

## リポジトリ外への指摘 (docs 変更ではないが重要)

### 27. `~/.claude/work-style.md` の effort 運用方針が公式既定と乖離

- 現状 (グローバル個人ルール): 「**デフォルト effort は `xhigh`**（Opus 4.7 で公式デフォルト）」
  + メモリ `feedback_effort_operation_policy`「デフォルト high・深掘り時 xhigh。JARVIS は `/effort xhigh` 切替を提案する」
- 公式最新:
  1. **Opus 5 の effort 既定は `high`** であり、公式推奨は「high 起点 + low/medium を主制御」
     (Opus 4.7 / 4.8 の xhigh 起点から反転済み。これは状態ファイルに既知として記録されている)
  2. 2026-08-14 ブログが「**`/model` と `/effort` は会話冒頭で決めよ。途中変更は prompt cache を壊す**」と明示
- JARVIS の見解: work-style.md の記述は Opus 4.7 時代のもので**既に古い**。加えて「JARVIS が途中で
  `/effort xhigh` 切替を提案する」という運用は、公式のトークン効率指針と真っ向から衝突する
- 提案: (a) work-style.md を「**既定 high・切替はセッション冒頭で行う**」に改訂し、
  (b) メモリ `feedback_effort_operation_policy` を同内容に更新する。
  ただし**本スキルのスコープは本リポジトリの `docs/` のみ**のため、実施は BOSS の承認後に別作業として行う
- 出典: https://claude.com/blog/maximizing-the-value-of-your-claude-code-sessions /
  https://platform.claude.com/docs/en/build-with-claude/effort /
  https://claude.com/blog/claude-model-and-effort-level-in-claude-code

---

## ドリフト検査の結果

- `docs/*.md` の実在ファイルと `CLAUDE.md`「ドキュメント構造」ツリー / 「ドキュメント参照」リスト: **ドリフト 0 件**
- `README.md` の目次テーブル (英 / 日): **ドリフト 0 件**
- commands ページのコマンド roster と `docs/slash-commands.md` の表: **掲載漏れ 0 件・削除漏れ 0 件**
  (`/pr-comments` `/vim` の廃止記録も正しい)

---

## 承認のお願い

上記のうち、反映してよい項目を番号でお知らせいただきたい。
(例: 「全部」「1〜12 のみ」「27 は却下、他は OK」など)

JARVIS の推奨は以下の通り:

| 優先度 | 項目 | 理由 |
|---|---|---|
| **必須** | 1・2・3 | 事実誤りであり、読んだ BOSS が誤った設定判断をする |
| **高** | 4・5・6・11・12・17 | セッション挙動に直結する新仕様の欠落 |
| **中** | 7〜10・13〜16・18・19・20 | 実運用で効くが緊急性は低い |
| **低 (記録のみ)** | 21〜26 | deferred 解消の記録・変化なしの確認 |
| **別作業** | 27 | 本リポ外 (`~/.claude/`) のため、承認後に別途対応 |
