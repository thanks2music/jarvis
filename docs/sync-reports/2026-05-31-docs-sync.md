# ClaudeCode ドキュメント同期レポート（item 15: slash-commands 新コマンド追記）

> 調査日: 2026-05-31
> 対象: `docs/slash-commands.md`（既存追記のみ。新規ファイルなし）
> 正ソース: 英語版 (en) — https://code.claude.com/docs/en/commands
> 範囲: 前回見送り項目 15。現行 doc に未記載の新コマンドを公式コマンド一覧と照合して抽出

## サマリ

現行 slash-commands.md に未記載の主要コマンドを、公式コマンドリファレンスと照合して抽出した。各グループは独立して採否判断できる。**クラウド連携コマンド（グループ 9）は item 14（クラウド機能群の新規ファイル）と密結合のため本フェーズでは見送りを推奨**する。

---

## 追記候補（番号で採否指定可）

### 1. コンテキスト管理に追記
- `/btw <question>` — コンテキストを汚さないサイドバー質問（回答は履歴に残らない）

### 2. セッション管理に追記
- `/resume [session]`（alias `/continue`）— ID / 名前 / ピッカーで会話を再開（bg セッションも表示）
- `/branch [name]`（alias `/fork`）— 現在の会話をこの地点で分岐。元会話は `/resume` で戻れる
- `/recap` — セッションの 1 行要約をオンデマンド生成
- `/plan [description]` — プロンプトから直接 plan mode に入る（任意のタスク指定可）

### 3. 新カテゴリ「モデル・推論制御」
- `/model [model]` — モデル切替（新セッションの既定として保存。`s` で当該セッションのみ）
- `/effort [level|auto]` — effort 設定（`low/medium/high/xhigh/max/ultracode`。`max`/`ultracode` は session-only、`auto` で既定に戻す）
- `/goal [condition|clear]` — 完了条件を設定し、達成まで Claude がターンを跨いで継続（`clear` 等で解除）

### 4. 環境確認・診断に追記
- `/config`（alias `/settings`）— 設定 UI（テーマ・モデル・出力スタイル等）
- `/status` — 設定 Status タブ（バージョン・モデル・アカウント・接続状況）
- `/usage`（alias `/cost`, `/stats`）— コスト・プラン使用量・スキル/subagent 別内訳
- `/statusline` — ステータスライン設定（自然言語指定 or shell プロンプトから自動構成）
- `/keybindings` — キーバインド設定ファイルの作成・編集
- `/diff` — 未コミット差分とターン別差分のインタラクティブビューア

### 5. 新カテゴリ「並列・バックグラウンド」
- `/background [prompt]` — セッションをバックグラウンドエージェント化してターミナルを解放
- `/tasks`（alias `/bashes`）— セッション内バックグラウンドタスクの一覧・管理
- `/workflows` — ワークフロー進捗ビュー（実行中/完了の監視・一時停止・保存）

### 6. バンドルスキルに追記
- `/code-review [low|medium|high|xhigh|max|ultra] [--fix] [--comment] [target]` — diff のバグ検出 + クリーンアップ。`ultra` でクラウド多エージェントレビュー
- `/run` — プロジェクトのアプリを起動して変更を実機確認（テストだけに頼らない）
- `/verify` — 変更がアプリ上で意図通り動くかをビルド・実行して検証
- `/deep-research <question>` — Web 横断調査 + 出典クロスチェック + 出典付きレポート（Workflow）
- `/security-review` — ブランチ差分のセキュリティ脆弱性分析（injection・auth 等）
- `/review [PR]` — PR をローカルでレビュー（深いクラウドレビューは `/code-review ultra`）
- `/fewer-permission-prompts` — transcript を走査し read-only Bash/MCP の allowlist を提案
- `/reload-skills` — スキル/コマンドディレクトリを再スキャン（再起動不要、v2.1.152+）

### 7. UI/表示に追記（軽め）
- `/theme` — カラーテーマ変更（`auto`・colorblind 対応等）
- `/focus` — フォーカスビュー切替（旧 `Ctrl+O`）
- `/tui [default|fullscreen]` — classic / flicker-free レンダラ切替

### 8. 廃止コマンドの注記（任意）
- `/vim`（v2.1.92 廃止 → `/config` の Editor mode）/ `/pr-comments`（v2.1.91 廃止）。現行 doc には未記載のため「廃止済みコマンド」注記として 1 行追加するか、追加せず放置かを選択。

---

## 9. クラウド連携コマンド（item 14 に委譲・本フェーズ見送り推奨）

以下は Ultraplan / Ultrareview / Routines / Claude Code on the web に密結合のため、item 14（クラウド機能群の新規ファイル）で本体解説と合わせて扱うのが整合的。slash-commands.md には item 14 着手時に「コマンド表 + 詳細は新規ファイル参照」の形で追加する。

- `/ultraplan <prompt>`、`/ultrareview [PR]`、`/schedule`（routines）、`/teleport`、`/remote-control`、`/autofix-pr`、`/web-setup`、`/remote-env`、`/insights`

---

## 連動更新
- slash-commands.md 末尾の「コマンド対応表（まとめ）」にも追加分を反映。
- `> 出典` 行の日付を 2026-05-31 に更新。
- 新規ファイルは作らないため README / CLAUDE.md 目次の変更は不要。

## 承認のお願い
反映してよいグループを番号でお知らせください（例: 「1〜7 を反映、8 は注記追加、9 は見送り」）。私の推奨は **1〜7 を反映 + 8 は廃止注記を 1 行追加 + 9 は item 14 へ委譲** です。

---

## 反映ログ (2026-05-31)

BOSS 承認（全グループ 1〜9）に基づき、`docs/slash-commands.md` に全コマンドを追記した。

### 反映内容
- **既存カテゴリに追記**: コンテキスト管理（`/btw`）、セッション管理（`/resume`・`/branch`・`/recap`・`/plan`）、環境確認・診断（`/config`・`/status`・`/usage`・`/statusline`・`/keybindings`・`/diff`）
- **新カテゴリを新設**: 「モデル・推論制御」（`/model`・`/effort`・`/goal`）、「並列・バックグラウンド」（`/background`・`/tasks`・`/workflows`）、「クラウド連携」（グループ 9 の全 9 コマンド: `/ultraplan`・`/ultrareview`・`/schedule`・`/teleport`・`/remote-control`・`/autofix-pr`・`/insights`・`/web-setup`・`/remote-env`）、「UI・表示」（`/theme`・`/focus`・`/tui`）
- **バンドルスキル追記**: `/code-review`・`/run`・`/verify`・`/deep-research`・`/security-review`・`/review`・`/fewer-permission-prompts`・`/reload-skills`
- **廃止注記**: `/vim`（v2.1.92）・`/pr-comments`（v2.1.91）
- 「コマンド対応表（まとめ）」に全追加分を反映、出典日付を 2026-05-31 に更新

### 方式
すべて既存追記（新規ファイルなし）。冗長な個別節ではなくカテゴリ別の簡潔なテーブルで追記し、doc の肥大化を抑えた。README / CLAUDE.md 目次の変更は不要。

### 横断自己検証
新コマンドの重複定義なし、対応表のテーブル整合 OK（`/tui` の `\|` はエスケープ済みで正常）。

### 補足（item 14 との関係）
グループ 9（クラウド連携コマンド）も BOSS 指示により slash-commands.md に追記済み。item 14（クラウド機能群の本体解説の新規ファイル化）では、これらコマンドの詳細解説を新規ファイルに置き、slash-commands.md からは相互リンクする方針とする。

### 採用した出典 URL
- https://code.claude.com/docs/en/commands
