# ClaudeCode ドキュメント同期レポート

> 調査日: 2026-07-26
> 前回調査: 2026-07-11 (ClaudeCode v2.1.207 時点)
> ローカル CLI: **v2.1.220** (2026-07-25 リリース)
> 正ソース: 英語版 (en)

## サマリ

- **新機能**: 18 件 (新規ファイル 0 件 / 既存追記 18 件)
- **更新必要**: 20 件 (うち**明確な事実誤り 3 件**)
- **ドリフト是正**: 3 件
- **要判断 (公式ソース間の不整合)**: 3 件
- **合計**: 41 項目 + 要判断 3 件

### 今回の最大の変化

**Claude Opus 5 (`claude-opus-5`) が 2026-07-24 に GA**（ClaudeCode は v2.1.219 以降が必須）。現在の JARVIS `docs/` には **Opus 5 の記述が一切存在しない**（`grep "Opus 5"` → 0 件）。加えて Opus 5 は「自己検証を自発的に行う」「subagent へ過剰に委任する」という性質を持つため、**公式が旧世代向けプロンプト作法の一部を「削除せよ」と明示している**。これは `docs/best-practices.md` / `docs/harness.md` の指針そのものに逆方向の修正を要求する、単なるバージョン追記ではない変更である。

さらに `docs/slash-commands.md` の `/fork` の説明は **v2.1.212 で仕様が変わったため現在は誤り**、`docs/model-comparison.md` の Fable 5 プロモ終了日は **実際の終了日 (2026-07-19) と異なる**。

---

## 【最優先】明確な事実誤り (3 件)

先に、現在のドキュメントが読者を誤らせる箇所を挙げる。

| # | ファイル | 誤っている記述 | 正しい記述 |
|---|---|---|---|
| **23** | `docs/slash-commands.md` L148 | `/fork <directive>` = 「会話全体を継承する **forked subagent** を spawn し、完了時に結果が会話へ戻る」 | v2.1.212 で再定義。`/fork` = **会話を新しい background セッションへ複製**（`claude agents` に独自行）。旧挙動は **`/subtask`** に改名 |
| **8** | `docs/model-comparison.md` §5 全体 | Fable 5 プロモは「2026-07-01 〜 **07-12** 23:59:59 PT (5 日延長)」で終了 | 実際の終了日は **2026-07-19 23:59:59 PT**（さらに 7 日延長されていた）。終了後の扱いもプラン別に確定済み |
| **10** | `docs/best-practices.md` L22 | 「effort 設定（**`xhigh` がデフォルト**）」 | 公式は「effort をサポートする全モデルで既定は **`high`**、例外は Opus 4.7 のみ `xhigh`」。既定モデルが Opus 5 になった現在、実効既定は `high` |

---

## A. モデル層 — Claude Opus 5 (2026-07-24 GA)

### 1. [更新必要] model-comparison.md — 現行モデル一覧に Opus 5 を追加、Opus 4.8 以前を Legacy へ

- 該当箇所: §2「現行モデル一覧 (2026-07-11 時点)」表 / §2 ※2 / Legacy 注記
- 現状: 列が Fable 5 / **Opus 4.8** / Sonnet 5 / Haiku 4.5。Opus 4.7 以下を Legacy 扱い
- 公式最新:
  - **Opus 5 (`claude-opus-5`)** を現行列に追加。$5 / $25 per MTok（Opus 4.8 と同額）、**1M context が既定かつ最大**（小さい context variant なし）、max output 128k、reliable knowledge cutoff **May 2026**（Opus 4.8 は Jan 2026）、Extended thinking No / Adaptive thinking Yes、Web fetch 非対応 / Priority Tier 非対応。Bedrock `anthropic.claude-opus-5` / Google Cloud `claude-opus-5` / Microsoft Foundry
  - **Opus 4.8 / 4.7 は公式 overview の Legacy models アコーディオンへ移動済み**
  - 300k output beta (`output-300k-2026-03-24`) の対象に **Opus 5** を追加（公式現行: Opus 5, 4.8, 4.7, 4.6, Sonnet 5, Sonnet 4.6）
  - Opus 4.1 は「2026-08-05 で deprecation 予定」ではなく **既に deprecated 済み、2026-08-05 は retirement 日**
  - 公式推奨文が「start with **Claude Opus 5** for complex agentic coding and enterprise work」に更新
- 対応案: Opus 4.8 の列・記述は削除せず Legacy 側へ移動（履歴保全）。Opus 5 列を追加
- 出典: https://platform.claude.com/docs/en/about-claude/models/overview / https://platform.claude.com/docs/en/about-claude/models/whats-new-opus-5 / https://www.anthropic.com/news/claude-opus-5 / https://platform.claude.com/docs/en/about-claude/model-deprecations

### 2. [更新必要] model-comparison.md / slash-commands.md — エイリアス解決とデフォルトモデル

- 現状: Opus 4.8 が default 前提の記述
- 公式最新（v2.1.219 以降）:
  - `opus` → **Opus 5**（Anthropic API / Claude Platform on AWS / Bedrock / Google Cloud すべて）
  - `default` → **Max / Team Premium / Enterprise PAYG / Anthropic API / Claude Platform on AWS / Bedrock / Google Cloud = Opus 5**、**Pro / Team Standard / Enterprise subscription seat = Sonnet 5**、Microsoft Foundry = Sonnet 4.5
  - `sonnet` → Anthropic API: Sonnet 5 / Claude Platform on AWS: Sonnet 4.6 / Bedrock・Google Cloud: Sonnet 4.5
  - **Opus 5 の利用には ClaudeCode v2.1.219 以上が必須**
  - Fable 5 はどの account type でも default ではない
- 出典: https://code.claude.com/docs/en/model-config

### 3. [更新必要] model-comparison.md — Opus 5 の thinking 既定 ON（**breaking change**）

- 現状: §3.3 Adaptive thinking の記述が Opus 4.8 世代前提
- 公式最新: Opus 4.8 からの破壊的変更が 2 点
  1. **thinking が既定 ON**。`thinking` 未指定でも adaptive thinking が走る。`max_tokens` は「thinking + response」合計に対する hard limit なので再調整が必要
  2. **thinking の無効化は effort `high` 以下でのみ許可**。`thinking:{"type":"disabled"}` × effort `xhigh`/`max` は **400 エラー**
  - thinking 無効時の既知アーティファクト: tool call が構造化 `tool_use` でなくテキストとして漏れる / `<thinking>` 等の内部 XML タグが可視出力に混入。**回避策は thinking を有効に保ったまま effort を下げること**（thinking ON + `low` は thinking OFF と同コストで高性能、と公式が明記）
- 出典: https://platform.claude.com/docs/en/about-claude/models/whats-new-opus-5 / https://platform.claude.com/docs/en/about-claude/models/migration-guide / https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5

### 4. [新機能・追記] model-comparison.md / slash-commands.md — Opus 5 の fast mode

- 要点: fast mode は research preview で **Claude API のみ**（Bedrock / Google Cloud / Foundry 非対応）、**$10 / $50 per MTok**（2.5x 速度 / 2x 価格）。v2.1.219 で `/fast` の対象が **Opus 5 と Opus 4.8** になり、**Opus 4.7 は fast mode から削除**（2026-06-25 deprecated → 2026-07-24 削除）
- ※ この項目は要判断 **J-3** を含む（後述）
- 出典: https://code.claude.com/docs/en/fast-mode / https://platform.claude.com/docs/en/about-claude/models/whats-new-opus-5

### 5. [新機能・追記] model-comparison.md — Opus 5 同時の API 側変更

- 要点: **prompt cache の最小長が 1,024 → 512 トークン**（Opus 5）、**mid-conversation tool changes beta**（`mid-conversation-tool-changes-2026-07-01`、prompt cache を保ったまま turn 間で tool を追加・削除）、**`fallbacks: "default"` モード**（`server-side-fallback-2026-07-01`）
- 出典: https://platform.claude.com/docs/en/about-claude/models/whats-new-opus-5

### 6. [更新必要] model-comparison.md / harness.md — classifier fallback がカテゴリ別に変更

- 現状: 「安全分類器がフラグ → default Opus に fallback（API は Opus 4.8、Claude Platform on AWS は Opus 4.7）」＝一律 fallback
- 公式最新（**v2.1.219 以降**）:
  - **Fable 5**: biology フラグ → **Opus 5** で再実行 / cybersecurity フラグ → **Opus 4.8** で再実行
  - **Opus 5**: cybersecurity フラグ → **Opus 4.8** で再実行 / **biology フラグは fallback なしで refusal 確定**（Opus 5 自身が biology classifier を持つため）
  - v2.1.219 未満は「flagged な Fable 5 → provider 既定 Opus に一律再実行」で、Opus 5 は fallback 先ではなかった
- 出典: https://code.claude.com/docs/en/model-config

### 7. [更新必要] model-comparison.md / best-practices.md — effort の推奨が Opus 4.7/4.8 から反転

- 現状: 「Opus 4.7 = `xhigh` 起点」を軸にした記述
- 公式最新:
  | モデル | 公式の出発点 |
  |---|---|
  | Opus 4.7 / 4.8 | `xhigh` から始める |
  | **Opus 5** | **`high`（既定）から始める。`low`/`medium` をコスト・レイテンシの主制御として積極的に使う。demanding な coding/agentic で `xhigh`、正当化できる場合のみ `max`** |
  - 「**旧モデルから effort 設定を引き継いだ場合は、自分の eval で effort sweep をやり直せ**」と明記
  - **Opus 5 には model-default hold が無い**。Fable 5 / Opus 4.8 / 4.7 は初回起動時にモデル既定 effort を強制適用して保持するが、Opus 5 は**以前設定したレベルをそのまま引き継ぐ** → 旧 `xhigh` 設定が黙って持ち越される
  - `xhigh` / `max` 実行時は `max_tokens` を大きく（64k 起点）
- **BOSS への注記**: グローバル個人ルール `~/.claude/work-style.md` の「デフォルト effort は `xhigh`（Opus 4.7 で公式デフォルト）」は、Opus 4.7 世代の指針である。Opus 5 では公式ガイダンスと不一致になる。docs 反映とは別に、**個人ルール側の見直しも検討に値する**（本レポートの承認対象外。別途ご判断ください）
- 出典: https://platform.claude.com/docs/en/build-with-claude/effort / https://code.claude.com/docs/en/model-config

### 8. [更新必要・**事実誤り**] model-comparison.md — Fable 5 プロモの終了日を訂正

- 該当箇所: L6 / L20 / §5 全体（L148, 160-162, 168, 189）
- 現状: 「2026-07-01 〜 **07-12** 23:59:59 PT（当初 07-07 → 5 日延長）」
- 公式最新: 公式 support 記事は「that promotion **ends on July 19, 2026 at 11:59:59 PM PT**」と過去形で記載。**実際の終了日は 2026-07-19 23:59:59 PT**（当初 07-07 → 07-12 → 07-19 と 2 回延長された形）。**延長も恒久化もされず終了**
- 終了後のプラン別扱い（確定情報）:
  | プラン | promotion 後の Fable 5 |
  |---|---|
  | **Max** | プランの標準機能として継続。**weekly limit の 50% まで追加費用なし** |
  | **Team premium seat** | Max と同じ |
  | **Pro** | **プランの usage limit に含まれない。usage credits が必要** |
  | **Team standard seat** | Pro と同じ |
  - 対象の Pro / Team standard seat には **one-time credit** が付与される
- 対応案: 時系列テーブル（L160-162）に 07-19 行を追加し、現状の「07-12 が最終」記述を訂正。§5 のタイトルと期間表記も更新
- 出典: https://support.claude.com/en/articles/15424964-claude-fable-5-promotional-access

### 9. [新機能・追記] best-practices.md — §8.5「Claude Opus 5 への更新」を新設

- 追記先: 第 8 章（§8.1 Opus 4.7 → §8.2 Opus 4.8 → §8.3 Fable 5 → §8.4 Sonnet 5 の後）
- 要点（既存 4 節と同形式、**置換ではなく追記**）: `claude-opus-5`、要 v2.1.219 以上、$5/$25、1M 既定、knowledge cutoff May 2026、effort 既定 `high` かつ hold なし、fast mode 既定モデル、`opus` エイリアスの解決先
- 能力変化（Opus 4.8 比、公式表現）: agentic coding / long-horizon が最大の伸び（stub や placeholder を残さず multi-file feature / 大規模 refactor を完遂）、**test-time compute scaling が歴代 Opus で最良**、`low`/`medium` が少ないトークンで高品質、code review の実バグ検出率が高く false positive が少ない（低 effort でも精度が落ちない）、multi-agent coordination で writer-verifier パターンが機能、vision / 1M 全域の instruction following 強化。**cybersecurity exploitation では Mythos 5 に劣る**
- 注意: **best-practices.md は重要文書のため、特に慎重な確認をお願いします**
- 出典: https://www.anthropic.com/news/claude-opus-5 / https://platform.claude.com/docs/en/about-claude/models/whats-new-opus-5

### 10. [更新必要・**事実誤り**] best-practices.md — effort デフォルト記述の Opus 5 対応

- 該当箇所: L22（「`xhigh` がデフォルト」）/ L565 / L760（モデル別デフォルト表）/ L766 / L770 / L827
- 現状: L760 のモデル別表は正確だが、**L22 の「effort 設定（`xhigh` がデフォルト）」は単独で読むと誤り**。L766/770/827 も Opus 4.7/4.8 前提
- 公式最新: 「The default effort is **`high`** on every model that supports effort, **except Opus 4.7, which defaults to `xhigh`**」。L760 の表に Opus 5 行（`high`、hold なし）を追加し、L22 の断定を修正
- 出典: https://code.claude.com/docs/en/model-config / https://platform.claude.com/docs/en/build-with-claude/effort

### 11. [更新必要] best-practices.md — subagent delegation の方針が**反転**

- 該当箇所: L324 付近「Opus 4.7 は subagent spawn が控えめ → 明示的に指示せよ」
- 公式最新: **Opus 5 は逆に委任が過剰になりがち**。公式の抑制推奨文言は「大規模かつ真に独立・並列化可能な作業のみ委任。数回の tool call で終わる作業は委任しない。**自分の作業の verify / double-check に subagent を使わない**。1 体で足りるなら 1 体」。明示的な委任基準か決定論的な spawn 上限を置くこと
- 併記すべき新事実: spawn には 3 種のハード上限が入った（per-session 200 / concurrent 20 / depth）ため、暴走ファンアウトはランタイム側でも抑止済み。`--max-budget-usd` は background subagent も停止させる（v2.1.217 修正）
- 対応案: Opus 4.7 の記述は履歴として残し、「Opus 5 では逆」という対比で追記
- 出典: https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5 / https://code.claude.com/docs/en/sub-agents

### 12. [更新必要] best-practices.md / harness.md — 検証指示の扱いが**二方向で変化**

- 公式最新（相反する 2 つの変化を同時に整理する必要がある）:
  1. **プロンプト側**: Opus 5 は指示なしで自己検証する。「最後に verification step を入れる」「subagent で verify させる」「double-check your answer」等の**旧世代由来の指示は over-verification を招くため削除せよ**と公式が明示。公式は「**legacy harness scaffolding が追加する別 verification step**」も削除対象に名指ししている
  2. **ハーネス側**: ClaudeCode は **v2.1.215 で `/verify` と `/code-review` の自発起動を停止**、**v2.1.218 で `/deep-research` も手動起動のみ**に変更。つまり検証スキルは**自分でチェーン / 埋め込みを組まないと走らない**
- → 「Claude 自身への verify 指示は減らす」が、「ハーネス側の明示ステップは自分で組む」という整理が必要
- 出典: https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5 / CHANGELOG v2.1.215, v2.1.218

### 13. [新機能・追記] best-practices.md — Opus 5 の冗長性・narration・スコープ制御

- 要点（すべて公式 prompting guide の明示項目）:
  - **応答が長くなる**。effort を下げても可視応答は短くならないため、**長さは明示プロンプトで制御**する
  - **ディスクに書く成果物も長くなる**。「filler / redundant summary / boilerplate で埋めない」旨の長さ校正指示を追加する
  - **進捗 narration が増える**。cadence を明示指定して調整する
  - **スコープを勝手に広げる**傾向。狭いタスクには「Deliver what was asked, at the scope intended.」型の明示制約を置く
- 出典: https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5

### 14. [更新必要] harness.md — モデル世代テーブルに Opus 5 行を追記 + Fable 5 fallback 修正

- 該当箇所: L293-296「モデル別ハーネス適性」表 / L300 Fable 5 補足
- 対応案:
  - **Opus 5 行を追記**（既存 4 行は残す）。位置づけは「**検証ステップをハーネスから外す方向の世代**」。公式が「legacy harness scaffolding が追加する別 verification step を削除せよ」と明言した点が最重要
  - L300 の「サイバー/生物関連タスクは自動で default Opus に fallback（API は Opus 4.8、AWS は Opus 4.7）」を項目 6 のカテゴリ別マトリクスに書き換え。Evaluator の能力前提の議論も新マトリクスに合わせる
- 出典: https://code.claude.com/docs/en/model-config / https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5

---

## B. サブエージェント / ハーネス runtime

### 15. [更新必要] sub-agents.md — nested subagent のデフォルトが変わった（★ 要判断 J-1）

- 該当箇所: L94「Nested subagents（入れ子）」
- 現状: 「`tools` に `Agent` を含めると有効」「**v2.1.181 以降は fg / bg 両方とも depth 5 でハード cap**」
- 公式最新: **v2.1.217 で nesting が既定無効（depth 1）に反転** → **v2.1.219 で既定 depth 3**。公式 docs 側は「From v2.1.172 through **v2.1.216**, subagents could nest by default, up to five layers deep, and the limit couldn't be changed」と履歴を明記しつつ、現行既定を「nesting 無効」と記載しており **CHANGELOG v2.1.219 と食い違う**（→ J-1）
- 併せて反映すべき事実: nesting 無効の間、`Agent` は fork 以外の全 subagent から withheld される（list には残るがエラーを返す）。有効化は `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH` に許可レイヤ数を設定
- 出典: https://code.claude.com/docs/en/sub-agents / CHANGELOG v2.1.217, v2.1.219

### 16. [新機能・追記] sub-agents.md — subagent spawn の 3 種ハード上限

- 要点:
  - **per-session cap 200**（`CLAUDE_CODE_MAX_SUBAGENTS_PER_SESSION`、v2.1.212+、無効化不可）
  - **concurrent cap 20**（`CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS`、v2.1.217+、超過時 `Concurrent subagent limit reached`、**ultracode セッションは免除**）
  - **depth cap**（`CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH`、v2.1.217+）
  - セッション全体の **WebSearch も 200 回上限**（`CLAUDE_CODE_MAX_WEB_SEARCHES_PER_SESSION`、v2.1.212+）
- 出典: https://code.claude.com/docs/en/sub-agents / CHANGELOG v2.1.212, v2.1.217

### 17. [新機能・追記] sub-agents.md / slash-commands.md / session-history.md — `/subtask` と `/fork` の分離

- 要点（v2.1.212）:
  - **`/subtask <instruction>`** = 従来 `/fork` が担っていた**会話内 forked subagent**。per-session budget は消費するが cap に blocked されない。agent view 無効時は `/subtask` が使えず `/fork` が旧挙動に戻る
  - **`/fork`** = 会話を**新しい background セッションに複製**。`claude agents` に独自行を持ち、作業を継続できる。名前はプロンプト先頭から自動命名。**subagent cap にカウントされない**
- 出典: https://code.claude.com/docs/en/sub-agents / https://code.claude.com/docs/en/whats-new/2026-w29 / https://code.claude.com/docs/en/commands

### 18. [更新必要] sub-agents.md — frontmatter / tool 仕様の細目

- `Task` / `Agent` tool の **`mode` パラメータが deprecated（無視される）**。subagent は親セッションの permission mode を既定継承（v2.1.212）
- agent markdown の **`name` に `:` を含められない**（plugin namespacing 予約、v2.1.218）
- **`subagentStatusLine` payload に reasoning effort が追加**（v2.1.214）
- `model` frontmatter のフル ID 例が `claude-opus-4-8` / `claude-fable-5` → 公式例は **`claude-opus-5`**
- L538 モデル表に **Opus 5 行が欠落**（$5/$25、1M、default Opus）
- 出典: CHANGELOG v2.1.212, v2.1.214, v2.1.218 / https://code.claude.com/docs/en/sub-agents

### 19. [更新必要] harness.md / config-files.md — Dynamic Workflows の既定サイズ変更（★ 要判断 J-2、実機で裏取り済）

- 該当箇所: harness.md §4.7（「ultracode = 1 セッションで数百の並列 subagent」）/ config-files.md L206
- 公式最新（v2.1.219）: dynamic workflows の既定が **medium size guideline（agent 15 体未満を目標）** に変更。設定キー **`workflowSizeGuideline`**（任意の settings ファイルから設定可。設定時は `/config` の該当行が hidden）。実行中 workflow の status line に現在の size を表示。**「数百の並列 subagent」は既定挙動ではなくなった**
- ※ 公式 workflows docs はまだ「`unrestricted` … This is the default」と記載（→ J-2）。ただし**本セッション (v2.1.220) の実機 Workflow tool 定義に「This session has the default workflow size guideline: medium — keep workflows under 15 agents.」と明記されており、CHANGELOG 側が正しいと実機で確認できる**
- 併せて反映: runtime 制約（同時 16 agent / 1 run 1,000 agent / 4,096 items / 25 agent または 150 万トークン超で `Large workflow` 警告）、subagent は常に `acceptEdits` で走る、保存先 `.claude/workflows/` と `~/.claude/workflows/`、plugin 配布は `workflows/`、**`ultracode` キーワードは人間入力起点のみ**（v2.1.210。`-p` / SDK 非 human / scheduled task / webhook / PR コメントでは発火しない）
- 出典: CHANGELOG v2.1.210, v2.1.219 / https://code.claude.com/docs/en/workflows

### 20. [新機能・追記] harness.md — verification loop の 4 配置モデル（新規節）

- 追記先: harness.md に新規節（§4 系列）
- 要点（公式ブログ 2026-07-22）: **Standalone**（横断チェックを明示起動）/ **Embedded**（生成 skill 内に検証を埋め込む）/ **Chained**（skill が完了時に別 skill を呼ぶ。**Anthropic 社内は `/code-review` → `/simplify` → `/verify` → 独自デザインチェック**）/ **PR-wide**（全 PR で自動実行）
- 実務指針: 「毎週手で検証していること」を起点に skill 化する。**chain は token 消費が増えるため embedded を先に検証してから chain 化**。PR gate は安定してから。過度な早期自動化を避ける
- 項目 12 と接続（v2.1.215 で `/verify` `/code-review` の自動発火が停止したため、chain / embedded を自分で組まないと走らない）
- 出典: https://claude.com/blog/building-verification-loops-in-claude-code-with-skills

### 21. [新機能・追記] harness.md / best-practices.md — Anthropic 自身の大規模移行 orchestration パターン

- 要点（公式ブログ 2026-07-16。Bun の Zig→Rust 100 万行を 2 週間未満・API コスト $165,000・merge 後 regression 19 件）:
  - 中核原則: 「**You fix the process (loop) that produced the code**」— 個別ファイルではなくループを直す
  - 6 段: ①Rulebook + 依存マップ ②サンプルで mini-migration による stress test ③並列 translation（不確実箇所は TODO マーカー）④compile を orchestrated loop で回し fixer agent が解消 ⑤smoke test ⑥behavior matching
  - パターン: **mechanical work queue**（タスクをディスク上のファイル存在で決定 → resumable）/ **adversarial review + 不一致は arbitration へ escalate** / **build daemon**（高価な compile を直列化し安価な fix を並列化）/ **model stratification**（実装は小モデル、review と rule 作成に大モデル）
  - 検証は「**built-in referee**」= compiler / test suite / 元コードとの behavioral diff を使う（harness.md §4.4 スプリント契約の評価軸に一次情報として追記）
- 出典: https://claude.com/blog/ai-code-migration

### 22. [新機能・追記] harness.md — 検証優先の設計論（Datadog / Temper）

- 要点（公式ブログ 2026-07-21）: 「Agents already produce code faster than any team can review; the gap between what's generated and what's proven is where failure modes pile up」— **bottleneck は生成でなく検証**。agent には任意コードでなく**仕様 (specification)** を出させ、**決定論的 kernel が検証・実行**する。state machine をコードでなくデータにする。各 artifact は「頭に収まるサイズ」に保つ
- 併せて harness.md §4.2/4.3 の Generator + Evaluator 構成に注記: Opus 5 では「自分の作業の verify に subagent を使わせない」が公式推奨。**独立した adversarial reviewer は有効だが、self-verify 用の subagent は逆効果**という切り分けを明記する
- 出典: https://claude.com/blog/how-datadog-built-a-universal-machine-tool-for-claude-code

---

## C. コマンド

### 23. [更新必要・**事実誤り**] slash-commands.md — `/fork` の定義変更 + `/subtask` 追加

- 該当箇所: L147（`/branch` 注記）/ L148（`/fork`）
- 現状: 「`/fork <directive>`: 会話全体を継承する forked subagent を spawn し、directive を別働で実行。完了時に結果が会話へ戻る（v2.1.161〜）」→ **v2.1.212 以降は誤り**
- 公式最新: 公式 built-in commands 表では `/fork` = 「Copy current conversation into new background session」、`/subtask` = 「Hand side task to subagent」。**`/subtask` はコマンド表に未記載のため追加が必要**
- 出典: https://code.claude.com/docs/en/commands

### 24. [更新必要] slash-commands.md / skills.md — 検証系バンドルスキルの自発起動が停止

- 現状: 自動発火前提の記述が残存
- 公式最新: **v2.1.215** — Claude は `/verify` と `/code-review` を自発起動しない。**v2.1.218** — `/code-review` は **background subagent** として実行（レビュー作業が会話コンテキストを埋めない）+ stacked slash commands をレビュー対象として保持、`/deep-research` も手動起動のみ
- 出典: CHANGELOG v2.1.215, v2.1.218 / https://code.claude.com/docs/en/commands / https://code.claude.com/docs/en/workflows

### 25. [更新必要] slash-commands.md — `/fast` の対象モデル変更 + `/model` エイリアス

- `/fast` の対象は **Opus 5 と Opus 4.8**。Opus 4.7 は 2026-06-25 deprecated → **2026-07-24 削除**。fast mode の既定は v2.1.219 以降 **Opus 5**（v2.1.154〜218 は Opus 4.8）。料金は Opus 5 / 4.8 ともに $10/$50 per MTok（1M 全域フラット）
- `/model` picker: merged Opus 行が「Opus (1M context)」表示に、最新モデル名のみハイライト（v2.1.219）。Fable 行が「Requires usage credits」と誤表示される問題を修正
- ※ 要判断 **J-3** を含む
- 出典: https://code.claude.com/docs/en/fast-mode / CHANGELOG v2.1.219

### 26. [新機能・追記] slash-commands.md — 未記載コマンドの追加

- ローカル doc に記載が無いコマンド: **`/subtask`**（項目 23）/ **`/workshop`** / **`/design-sync`** / **`/design-login`** / **`/usage-credits`**
- ※ 他の新顔候補（`/batch` `/insights` `/passes` `/focus` `/heapdump` `/diff` `/claude-api` `/keybindings` `/install-slack-app` `/fewer-permission-prompts` `/radio` `/tasks` `/workflows` `/teleport` `/deep-research` `/verify`）は **doc 側には既に記載済み**で、state ファイルの `known_features` に載っていなかっただけ。state 側の記録漏れとして補完する
- 反映時は各コマンドの説明を公式 commands ページから取り直す。導入バージョンが CHANGELOG から確定できないものは版表記を付けない
- 出典: https://code.claude.com/docs/en/commands

### 27. [新機能・追記] slash-commands.md — 対話系の細目

- **`claude auto-mode reset`**（+ `--yes` で確認スキップ）— auto mode 設定を既定へ復元（v2.1.212）
- **agent view 内の `/resume`** が過去セッション（削除済み含む）のピッカーを開き、**background セッションとして再開**（v2.1.212）
- **引数なしの `/btw`** で直近やり取りの side-question パネルを再オープン（v2.1.212）
- **`claude --teleport` / `/teleport`** — web セッションを現ターミナルへ引き込む。v2.1.219 でリポ不一致時の表示改善（導入バージョンは未確認）
- 出典: https://code.claude.com/docs/en/whats-new/2026-w29 / https://code.claude.com/docs/en/commands / CHANGELOG v2.1.212, v2.1.219

---

## D. settings / permissions / 環境変数

### 28. [更新必要] config-files.md — `dynamicWorkflowSize (仮称)` → **`workflowSizeGuideline`** に確定

- 該当箇所: L206。現状は「(仮称)」「詳細キー名は要確認」と保留していた
- 公式最新: 正式キー名は **`workflowSizeGuideline`**（v2.1.219）。既定は medium（agent 15 体未満）。任意の settings ファイルから設定可
- 出典: CHANGELOG v2.1.219

### 29. [更新必要・**重要**] config-files.md — permissions の path rule 非推奨化（v2.1.210）

- 現状: `Tool(param:value)` 等は網羅済みだが、**tool 別の path rule 可否に言及なし**
- 公式最新: file permission check は **`Edit(path)` と `Read(path)` にのみマッチ**する。**`Write(path)` / `NotebookEdit(path)` / `Glob(path)` は accept されるが never matched** で、allow / deny / ask 各ルールごとに**起動時警告**が出る。移行は `Write(docs/**)` → `Edit(docs/**)`、`Glob(docs/**)` → `Read(docs/**)`。path なしの tool 名ルール（`Write` 単体 deny 等）は影響なし
- **BOSS への注記**: 本リポの `.claude/settings.json` を含め、既存の permission ルールに `Write(...)` / `Glob(...)` 形式が含まれていると**無効なルールとして黙って無視される**。doc 反映と併せて実 settings の点検を推奨する（点検自体を実施するかは別途ご指示ください）
- 出典: https://code.claude.com/docs/en/permissions / CHANGELOG v2.1.210

### 30. [新機能・追記] config-files.md — glob の深さ挙動の非対称化 + セキュリティ修正（v2.1.214）

- **ルール種別で分岐**: **allow** は `Edit(src/**)` が `<cwd>/src` のみマッチ（任意深さは `Edit(**/src/**)`）、**deny / ask** は `Read(secrets/**)` が任意深さの `secrets` にマッチ
- **v2.1.214 セキュリティ修正**: 単一セグメントの `dir/**` allow ルールが**ツリー内の任意の `dir/` への書き込みを自動承認していた**問題を修正。ほかに PowerShell 5.1 の permission チェック回避、fd リダイレクト形式の fail-closed 化、`file -m/-f` の権限要求化
- 出典: https://code.claude.com/docs/en/permissions / CHANGELOG v2.1.214

### 31. [新機能・追記] config-files.md — sandbox / auto mode / always allow

- **`sandbox.filesystem.disabled`**（v2.1.216）— filesystem 隔離のみ無効化しネットワーク隔離は維持。user / managed / `--settings` のみ設定可（project / local 不可）。`CLAUDE_CODE_SUBPROCESS_ENV_SCRUB` 設定時は全ソース無視
- **`sandbox.network.strictAllowlist`**（v2.1.219）— allowlist 外ホストをプロンプトなしで拒否（※ settings / sandboxing docs 未反映、出典は CHANGELOG）
- **auto mode**: permission classifier の既定が **Sonnet 5**（v2.1.210。allowlist が Sonnet 5 を許さない場合はセッションモデル / Opus。セッション初回リクエストで検証し以降 pin）/ **v2.1.218** — dangerous-`rm` / background-`&` / suspicious-Windows-path の各チェックが permission dialog を開かず **classifier が裁定**、plan mode + auto では静的解析で read-only と証明できない Bash のプロンプトを廃止（`useAutoModeDuringPlan`）/ **v2.1.216** — OAuth token 期限切れ時の「HTTP 401」classifier エラーで deny する不具合を修正
- **`disableAutoMode`** — `"disable"` で auto mode を封鎖（Shift+Tab サイクルから除去、`--permission-mode auto` を拒否）。managed settings 向け
- **「always allow」がリポジトリルートに保存**（v2.1.211）— git worktree で与えた承認がセッション / worktree を跨いで永続化
- 出典: https://code.claude.com/docs/en/sandboxing / https://code.claude.com/docs/en/permission-modes / https://code.claude.com/docs/en/settings / CHANGELOG v2.1.210, v2.1.211, v2.1.216, v2.1.218, v2.1.219

### 32. [新機能・追記] config-files.md — settings / env の追加とバージョン訂正

- **settings 追加**: `workflowSizeGuideline`（v2.1.219）/ `emojiCompletionEnabled`（既定 `true`、v2.1.217）/ `vimInsertModeRemaps`（v2.1.208、`jj` → Escape 等の 2 キー insert-mode リマップ）/ `processWrapper`（v2.1.210、企業ランチャー経由で自己 spawn プロセスを起動。project / local 不可）
- **env 追加**: `CLAUDE_CODE_PROCESS_WRAPPER`（v2.1.208）/ `CLAUDE_CODE_MAX_SUBAGENTS_PER_SESSION`（既定 200）/ `CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS`（既定 20）/ `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH` / `CLAUDE_CODE_MAX_WEB_SEARCHES_PER_SESSION`（既定 200）/ `CLAUDE_CODE_MCP_AUTO_BACKGROUND_MS` / `CLAUDE_CODE_FORWARD_SUBAGENT_TEXT`（v2.1.211）/ `CLAUDE_CODE_OTEL_CONTENT_MAX_LENGTH`（v2.1.214、既定 60KB）/ `CLAUDE_CODE_DISABLE_BEDROCK_CONTENT_TYPE_GUARD`（v2.1.208）/ `CLAUDE_AX_SCREEN_READER`
- **CLI フラグ追加**: `--ax-screen-reader` / `--forward-subagent-text` / `--max-budget-usd` / `--betas`
- **バージョン帰属の訂正**: L178 の `axScreenReader` は「v2.1.181〜」だが、**screen reader mode 自体は v2.1.208 の新機能**（`axScreenReader` キーのみ v2.1.181 から存在）。以降 v2.1.210/214/217/218 で読み上げ改善
- **managed 設定の細目**: managed MCP allowlist/denylist の `${VAR}` は **settings-file env ではなく startup environment + managed-settings env** から解決（v2.1.219）/ 無害な feature・cost トグルは settings-approval prompt を出さない（v2.1.218）/ `--settings` が 2 MiB 超で起動時エラー（v2.1.214）
- **`EndConversation` tool**（v2.1.214）— 極端に敵対的 / jailbreak 試行のユーザーに対し Claude がセッションを終了できる。**他ツールが残っている限り deny / ask ルールや `disallowed-tools` では除去できない**
- **corporate launcher の専用 docs ページが新設**: https://code.claude.com/docs/en/corporate-launcher
- 出典: https://code.claude.com/docs/en/settings / https://code.claude.com/docs/en/env-vars / https://code.claude.com/docs/en/accessibility / https://code.claude.com/docs/en/corporate-launcher / https://code.claude.com/docs/en/tools-reference / CHANGELOG

---

## E. hooks

### 33. [新機能・追記 + 更新必要] hooks.md — イベントと挙動の追加

- **`DirectoryAdded` hook（v2.1.219）** — `/add-dir` または SDK の `register_repo_root` control request がセッション中に新しい作業ディレクトリを登録した後に発火。⚠️ **公式 hooks ページのイベント表には未掲載**のため、反映時は出典を CHANGELOG と明記する
- **`SessionStart` の source に `fork` が追加**（v2.1.214）— 全 source は `startup` / `resume` / `clear` / `compact` / **`fork`**。`fork` は `--fork-session` + `--resume`/`--continue`、`/fork` の background copy、`/branch` 由来（従来は `resume` に含まれていた）。※ 現在の hooks.md には source 値の列挙が無いため新規追記
- **hook の `if:` 条件で single-segment glob の深さ挙動**（v2.1.214）— `"Edit(src/**)"` は `<cwd>/src` のみマッチ。任意深さは `"Edit(**/src/**)"`
- **exit code 2 のバグ修正**（v2.1.214）— stdout JSON の schema validation 失敗時にブロックしなかった問題を修正（ドキュメント通りブロックする）
- **agent frontmatter の hooks に workspace trust 要求**（v2.1.218）— その agent ファイル自身のフォルダが trust 承認済みであることを要求
- **PreToolUse hook の `ask` が auto mode に上書きされない**（v2.1.211）— hook の `ask` が最低で prompt を保証。`continue:false` の halt が tool 失敗 / mid-stream 完了で落ちるバグも修正（v2.1.212）
- 出典: https://code.claude.com/docs/en/hooks / CHANGELOG v2.1.211, v2.1.212, v2.1.214, v2.1.218, v2.1.219

---

## F. MCP

### 34. [更新必要 + 追記] mcp-setup.md — timeout の実像と信頼性改善

- 該当箇所: L220（5 分 idle timeout）/ L7-9（予約サーバー名）/ L215-220（信頼性改善）
- **現状の記述が不完全**: 「remote MCP tool call 5 分 idle timeout（v2.1.187）」だけでは実挙動を説明できない。公式は「これらの timeout は *call が走れる長さ* を縛るもので、*セッションをブロックする長さ* とは別」と整理している
- 公式最新: **v2.1.212 以降、メイン会話の MCP tool call が 2 分を超えると background task へ自動移行**し、Claude は即座に task ID を受け取って作業を継続、結果は task notification で届く。閾値は **`CLAUDE_CODE_MCP_AUTO_BACKGROUND_MS`**（ms、`0` で無効化）、`CLAUDE_CODE_DISABLE_BACKGROUND_TASKS=1` でも無効。非対話モードは対象外（`CLAUDE_AUTO_BACKGROUND_TASKS=1` で有効化）
- 予約サーバー名の現行リスト: `workspace` / `claude-in-chrome` / `computer-use` / `Claude Preview` / `Claude Browser`（`claude mcp add` は予約名をエラーで拒否）
- 追加すべき改善履歴: **v2.1.219** — `claude mcp list` / `/mcp` が接続失敗時に **HTTP status + error text** を表示、MCP 設定値の先頭 / 末尾の不可視空白に警告、headless stream-json の init event に **`mcp_server_errors`**（config validation でスキップされた `--mcp-config` エントリ）。**v2.1.216** — MCP 再認証が新サインイン成功前に有効な資格情報を revoke するバグを修正。**v2.1.217** — 切り詰めた MCP tool output が未切り詰め全文をセッション中メモリに保持し続けるリークを修正
- 出典: https://code.claude.com/docs/en/mcp / CHANGELOG v2.1.216, v2.1.217, v2.1.219

---

## G. skills

### 35. [新機能・追記] skills.md — `background` frontmatter と副作用

- 該当箇所: frontmatter 表（L116 付近）/ §「サブエージェント実行（context: fork）」L196-223
- 公式最新:
  - **`background` フィールド（v2.1.218）** — `context: fork` 時のみ有効、**既定 `true`** = forked subagent は **background で実行**され、結果は後から会話に届く。`background: false` で呼び出しターン内で待つ。v2.1.218 より前は forked skill が常にターンをブロックしていた
  - background 実行の副作用 3 点: ① background subagent の**狭いツールセット**が適用される（必要なら `background: false`）② background fork の編集はセッションの checkpoint 外なので **`/rewind` で戻せない**（git で revert）③ `CLAUDE_CODE_DISABLE_BACKGROUND_TASKS=1` 等では待ち動作になる
  - **frontmatter の boolean が `yes`/`no`/`on`/`off`/`1`/`0`（大小文字不問）も受理**（v2.1.218、skill / plugin 共通）
  - **セッション中に変更した skills / commands が再起動なしでスラッシュメニューに反映**（v2.1.216）
  - `disallowed-tools` では `EndConversation` を除去できない（項目 32 参照）
- 併せて L320-322 のバンドルスキル節に項目 24（自発起動停止）を反映
- 出典: https://code.claude.com/docs/en/skills / CHANGELOG v2.1.216, v2.1.218

---

## H. sessions / memory

### 36. [更新必要 + 追記] session-history.md — `/fork` 再定義と transcript の挙動

- **`/fork` の再定義（v2.1.212）は本 doc の主題（セッション同一性）に直結**。`/branch` と `--fork-session` で作ったセッションは独自の session ID を持ち picker に別行で出る。`/fork` のコピーは**独立した background セッション**
- **`SessionStart` hook の source が fork 起点で `"fork"`**（v2.1.214、従来 `"resume"`）。headless / SDK で compaction 後に fork-session の lineage が失われるバグを修正（v2.1.218）
- **`/loop` 起点セッションの picker 挙動変更（v2.1.211）** — 最初のプロンプトが `/loop` だったセッションは picker に出ない（会話途中の `/loop` では隠れない）。それ以前は会話初期の `/loop` が恒久的に picker から隠していた
- **transcript**: 各 assistant message に **reasoning effort level** を記録（v2.1.212/213）/ superseded な file-history backup の剪定で transcript サイズが最大 **79 倍**削減（v2.1.208）/ 書き込み失敗（ディスクフル等）や env 継承でセッション保存が off の場合に**警告**を出す（v2.1.217、無言で失う挙動を廃止）
- **`--forward-subagent-text` / `CLAUDE_CODE_FORWARD_SUBAGENT_TEXT`**（v2.1.211）で stream-json に subagent の text / thinking を含める。v2.1.219 で depth-2 以上の nested subagent も転送され、**spawn 元 Agent の `tool_use` id をキー**に紐づく
- 出典: https://code.claude.com/docs/en/sessions / CHANGELOG v2.1.208, v2.1.211, v2.1.212, v2.1.214, v2.1.217, v2.1.218, v2.1.219

### 37. [新機能・追記] memory.md — `modified` frontmatter と上限計測

- **`modified` frontmatter フィールド（v2.1.214）** — Claude がメモリファイルを書き込む際、そのファイルが YAML frontmatter で始まる場合、書き込み時刻を **ISO 8601 の `modified` フィールド**として記録する。旧版で作られたファイルも次回書き込み時に付与される。**frontmatter が無いファイルには決して frontmatter を追加しない**
- **上限計測の挙動** — 書き込み後に上限を計測し、近い場合は短縮を促し、超過時は書き込みは成功するが**明示エラー**を返す（v2.1.210、silent truncation ではない）。計測対象は**ロードされる内容のみ**で、YAML frontmatter と block-level HTML コメントは剥がされ上限にカウントされない（v2.1.211）
- 出典: https://code.claude.com/docs/en/memory / CHANGELOG v2.1.210, v2.1.211, v2.1.214

---

## I. ブログ由来のベストプラクティス（2026-07-24 公式）

### 38. [新機能・追記] best-practices.md / skills-progressive-disclosure.md / memory.md — 「context engineering の新ルール」

- 出典記事: [The new rules of context engineering for Claude 5 generation models](https://claude.com/blog/the-new-rules-of-context-engineering-for-claude-5-generation-models)（2026-07-24）
- 要点: Anthropic は先進モデル向けに **ClaudeCode の system prompt を 80% 以上削除して性能低下なし**。哲学が「制約する」→「判断を信頼する」へ移った
- Then → Now の 6 転換:
  1. **Rules → Judgment**（例: 「Write code that reads like the surrounding code」）
  2. **Examples → Interface Design**（tool パラメータの表現力で示す）
  3. **Upfront Info → Progressive Disclosure**（skills に逃がす）
  4. **Repetition → Concision**（system prompt と tool description の重複排除）
  5. **Manual Memory → Auto-memory**
  6. **Simple Specs → Rich References**（実コード・テストスイート・HTML artifact・rubric を渡す）
- ClaudeCode 実務: **CLAUDE.md は軽量に保ち gotcha とリポ固有の癖に絞る** / skills は progressive disclosure 前提でモジュール分割 / spec や mockup は `@mention` 参照 / **`/doctor` で skills と context ファイルを right-size する**
- **本リポとの整合**: 直近コミット `deed983`（CLAUDE.md の `@import` 廃止）は、この公式方針と**整合している**。best-practices.md の CLAUDE.md 節にこの裏付けを追記できる
- 反映先の内訳: 6 転換 + CLAUDE.md 軽量化 → `best-practices.md` / 3・2・4 の詳細 → `skills-progressive-disclosure.md` / 5 → `memory.md`（auto-memory が公式方針化した旨の注記）/ 6 → `best-practices.md` の「具体的コンテキスト」節（BOSS の既存ルール「第 1 ターンに完全な仕様を渡し切る」の公式裏付けにもなる）
- 注意: **best-practices.md は重要文書のため、特に慎重な確認をお願いします**

---

## ドリフト是正 (公式変化とは別: ドキュメント整合性)

### 39. [ドリフト] CLAUDE.md のドキュメント構造ツリーに未記載のディレクトリ

- `docs/diagrams/`（tracked 6 ファイル）と `docs/sync-reports/`（tracked 8 ファイル）が git 管理されているが、CLAUDE.md の「Documentation Structure」ツリーに未記載
- ※ `docs/constitution/` `docs/handoff/` `docs/ideas/` `docs/plan/` `docs/report/` `docs/skills/` は `.gitignore` 済みのため記載不要
- 対応案: ツリーに 2 ディレクトリを追記（1 行ずつ、役割コメント付き）
- ※ `docs/*.md` 18 ファイルと CLAUDE.md のリストは**完全一致しておりドリフトなし**（`zed-shortcuts.md` も記載済み）

### 40. [ドリフト] state ファイルの `known_features` に記録漏れ

- `docs/slash-commands.md` には記載済みだが state の `known_features` に載っていないコマンドが 16 件（項目 26 の ※ 参照）。次回調査で「新規」と誤検出されるため state 側を補完する
- 対応案: フェーズ2 の state 更新時にまとめて追記

### 41. [継続] 未精査 / 保留項目

- **`docs/plugins.md`**（今回優先度を下げたため公式 plugins 系 5 ページ未取得）。CHANGELOG 由来の反映候補のみ: agent 名の `:` 禁止（v2.1.218）、plugin frontmatter boolean の値拡張（v2.1.218）、`name` frontmatter 持ちの plugin skill が補完で plugin prefix を失う不具合修正（v2.1.216）、`--settings` 経由で有効化した plugin がロードされない不具合修正（v2.1.214）、sideload 系フラグの managed ブロック
- **`docs/claude-desktop.md`**（未精査）。反映候補: **v2.1.217** で corporate mTLS / TLS-verify / OAuth scope / proxy 設定が Claude Desktop セッションで無視されていた不具合を修正、**v2.1.212** で hosted（host-managed）セッションは mTLS 証明書・追加 CA バンドル・OAuth scope を**警告付きで無視**する仕様
- **既存 `deferred_items`**（前回から継続）: ① Agent view / Monitor tool の専用解説（新規 or 追記の方針判断が残る）② クラウド機能群（Ultraplan / Ultrareview / Routines / Claude Code on the web）の本体解説
- **Artifacts の Week 29 更新**（`slash-commands.md` の Artifacts 節候補）: 公開済み artifact が**閲覧者自身の MCP connector を呼べる**（閲覧ごとにライブデータ取得・アクション実行）、**public sharing links**、**editor role（Team / Enterprise）**、**Claude Tag セッションからの artifact 作成** — https://code.claude.com/docs/en/whats-new/2026-w29
- **Screen reader mode の詳細**（`config-files.md` or 新規）: 視覚 TUI をラベル付き線形テキストへ置換（VoiceOver / NVDA）。v2.1.208 導入 — https://code.claude.com/docs/en/accessibility

---

## ★ 要判断: 公式ソース間の不整合 (3 件)

JARVIS の調査ルールに従い、公式ソース間で仕様が食い違った件を選択肢付きで報告する。

### J-1. nested subagent の既定深さ

| ソース | 記述 |
|---|---|
| **CHANGELOG v2.1.219** | 「Subagents can now spawn nested subagents up to **depth 3 by default** (was 1); set `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH=1` to disable nesting」 |
| **公式 docs** [sub-agents](https://code.claude.com/docs/en/sub-agents) | 「**By default, a subagent can't spawn subagents of its own** … To allow nesting, set `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH` to the number of subagent layers you want」 |

- 両者が正反対。docs 側は「v2.1.172〜v2.1.216 は既定 depth 5」という履歴を min-version 注記付きで持っており記述は精密だが、**v2.1.219 の変更に追従していない可能性が高い**
- **私の推奨（選択肢 A）**: **CHANGELOG（v2.1.219 で既定 depth 3）を正として記述し、「公式 sub-agents docs は 2026-07-26 時点で『既定 nesting 無効』のままで不整合」と注記する**。根拠は 2 点 — ① CHANGELOG は同日リリースの一次情報で日付が新しい ② **同じ v2.1.219 の workflow size 変更（J-2）では CHANGELOG 側が正しいことを実機で確認済み**であり、v2.1.219 に対する docs 追従ラグが実証されている
- 選択肢 B: docs を正として「既定 nesting 無効」と書き、CHANGELOG の不整合を注記する
- 選択肢 C: 両方を併記し、断定を避けて「要再確認」とする

### J-2. Dynamic workflow の既定 size guideline

| ソース | 記述 |
|---|---|
| **CHANGELOG v2.1.219** | 「Changed dynamic workflows to default to a **medium size guideline (aim for fewer than 15 agents)**」 |
| **公式 docs** [workflows](https://code.claude.com/docs/en/workflows) | 「`unrestricted` … **This is the default**」 |
| **実機（本セッション v2.1.220）** | Workflow tool 定義に「This session has the default workflow size guideline: **medium** — keep workflows under 15 agents.」 |

- **私の推奨**: **CHANGELOG を正として反映する**。実機の tool 定義が CHANGELOG と一致しており、docs 側の追従ラグと断定できる。反映時は「公式 workflows docs は `unrestricted` のままだが、v2.1.220 実機では medium が既定」と注記する

### J-3. `/fast` における Opus 4.7 の扱い

| ソース | 記述 |
|---|---|
| **CHANGELOG v2.1.219** | 「**Removed Opus 4.7 from fast mode**; `/fast` now applies to Opus 5 and Opus 4.8」 |
| **公式 docs** [fast-mode](https://code.claude.com/docs/en/fast-mode) | Opus 4.7 の fast mode は 2026-06-25 deprecated → 2026-07-24 削除。**ClaudeCode は 4.7 を fast model 扱いし続けるが API が reject する** |
| **実機（本セッション v2.1.220）** | system prompt に「Fast mode … available on Opus **5/4.8/4.7**」 |

- **私の推奨**: 矛盾ではなく**同一事実の異なる側面**と判断する。「API 側では Opus 4.7 の fast mode は削除済み（reject される）が、ClaudeCode の UI / 内部説明はまだ 4.7 を fast 対象として提示する」と整理して記述するのが正確。実機の system prompt が docs の記述を裏付けている
- 選択肢: 「`/fast` の対象は Opus 5 と Opus 4.8 のみ」と単純化して書く（読みやすいが実機の表示と食い違う）

---

## 承認のお願い

上記のうち、反映してよい項目を番号でお知らせください。
（例: 「全部」「1〜14 のみ」「29 は却下、他は OK」など）

要判断 3 件（**J-1 / J-2 / J-3**）は、私の推奨で進めてよいかも併せてご指示ください（無指定なら推奨案で進めます）。

### 私の推奨する優先順位（分割される場合）

1. **最優先**: 事実誤り 3 件（**8 / 10 / 23**）— 現在の doc が読者を誤らせている
2. **高**: Opus 5 のモデル層（**1〜7 / 9 / 14**）— 世代交代そのものが未反映
3. **高**: Opus 5 由来のプロンプト / ハーネス方針の反転（**11 / 12 / 13 / 22**）— 従来指針と逆向きのため放置すると害がある
4. **中**: permissions の非推奨化（**29 / 30**）— 実 settings が黙って無効化される実害あり
5. **中**: `/subtask` 分離とサブエージェント上限（**15〜18 / 24 / 27**）
6. **中**: ブログ由来ベストプラクティス（**20 / 21 / 38**）
7. **低〜中**: settings / env / hooks / MCP / skills / sessions の細目（**19 / 25 / 26 / 28 / 31〜37**）
8. **低**: ドリフト是正と継続項目（**39 / 40 / 41**）
