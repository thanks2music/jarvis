# ClaudeCode ドキュメント同期レポート

> 調査日: 2026-08-04
> 前回調査: 2026-07-26 (ClaudeCode v2.1.220 時点)
> 正ソース: 英語版 (en)
> 実機バージョン: v2.1.221 / CHANGELOG 最新: v2.1.221 (v2.1.222 以降は未リリース)

## サマリ

| 区分 | 件数 |
|---|---|
| A. 誤った「公式未反映」警告の削除 (公式が追従済み) | 4 件 |
| B. 明確な事実誤り | 5 件 |
| C. 新機能・変更 (v2.1.221 由来) | 9 件 |
| D. 公式にあるが未記載の欠落 | 11 件 |
| E. モデル / ブログ由来 | 6 件 |
| F. 見送り継続 (公式で裏が取れず) | 2 件 |
| **合計** | **35 項目 (うち反映対象 35 / 見送り 2)** |

- **新規ファイル作成は 0 件**。すべて既存ファイルへの追記・修正で収まる。
- **whats-new 週次ページは Week 29 (07-13〜17) で止まっており、`2026-w30` / `w31` / `w32` はいずれも 404**。Opus 5 GA 週以降の digest は未公開のため、今回の新機能検出は **CHANGELOG が唯一の一次情報**である。
- **CLAUDE.md / README.md の目次ドリフトは 0 件** (実ファイルと完全一致を確認済み)。

> **今回の性格**: 前回 (2026-07-26) に「公式 docs が CHANGELOG に追従していない」として CHANGELOG を正とした判断が **4 件すべて公式側の追従で裏付けられた**。結果として、ローカル doc に残る「⚠️ 公式ソース間の不整合」注記が**全部が誤った警告**になっている。読者を誤らせるため優先度は最高。

---

## A. 誤った「公式未反映」警告の削除 (公式が追従済み)

### 1. sub-agents.md — nested subagent depth の「公式は追従していない」警告が不要になった

- 該当箇所: `docs/sub-agents.md` L106
- 現状: 「公式 Subagents は現行既定を『By default, a subagent can't spawn subagents of its own』と記載し、**v2.1.219 の変更に追従していない**」+ CHANGELOG を正とした判断根拠の長い説明
- 公式最新: 追従完了。「**By default, a subagent can spawn subagents of its own, up to three layers below the main conversation.**」。版履歴も「**v2.1.217 through v2.1.218**: the limit defaulted to one … v2.1.219 raised the default to three」と明記
- **旧キーワード**: `v2.1.219 の変更に追従していない` / `公式ソース間の不整合`
- **波及ファイル**: `docs/sub-agents.md` (L106 のみ。grep 実施済み)
- 対応案: L106 の不整合注記を削除し、「公式 docs も v2.1.219 の既定 depth 3 に追従済み (2026-08-04 確認)」に置換。L102 の depth 表の `v2.1.217` 行を **`v2.1.217 〜 v2.1.218`** に修正 (公式の版範囲表記に合わせる)
- 出典: https://code.claude.com/docs/en/sub-agents

### 2. slash-commands.md / harness.md / config-files.md — workflow size guideline の「CHANGELOG と食い違う」警告が不要になった

- 該当箇所: `docs/slash-commands.md` L451 / `docs/harness.md` L411 / `docs/config-files.md` L246
- 現状: 「公式 workflows ページは『`unrestricted` … This is the default』と記載しており CHANGELOG と食い違う」+ 実機確認を根拠に CHANGELOG を採用した旨
- 公式最新: 追従完了。「**The default is `medium`.** Until you choose a value, the `/config` row shows `medium (default)` … **Requires Claude Code v2.1.219 or later; earlier versions default to `unrestricted`**」
- 併せて判明した未記載事項:
  - **受理値は 4 種**: `unrestricted` (目安なし) / **`small` (5 体未満)** / `medium` (15 体未満) / **`large` (50 体未満)** — ローカルは `medium` と `unrestricted` の 2 値しか書いていない
  - 「Claude Code sends the guideline to Claude as **advice, not a cap**」 — プロンプト側が別スケールを要求すれば上書きされる
  - size guideline 機能自体は **v2.1.202 以降** (`workflowSizeGuideline` の settings キーが使えるのは v2.1.219 以降)
  - 自分で guideline を選ぶと `Large workflow` 警告の閾値 25 体が**その agent 数に置き換わる**。ultracode 有効時は警告が出ない
- **旧キーワード**: `This is the default」と記載しており CHANGELOG と食い違う` / `` `unrestricted` も選べる ``
- **波及ファイル**: `docs/slash-commands.md` (L451) / `docs/harness.md` (L411) / `docs/config-files.md` (L246) — 3 ファイル (grep 実施済み)
- 対応案: 3 ファイルの不整合注記を削除し、4 値表 + 「advice, not a cap」を追記
- 出典: https://code.claude.com/docs/en/workflows

### 3. hooks.md — `DirectoryAdded` の「公式イベント表に未掲載」注記が不要になった

- 該当箇所: `docs/hooks.md` L149
- 現状: 「⚠️ 出典は CHANGELOG v2.1.219。公式 hooks のイベント表には 2026-07-26 時点で**未掲載**」
- 公式最新: イベント表に掲載済み。「When a working directory is added mid-session via `/add-dir` or the SDK `register_repo_root` control request」。さらに **matcher 値** (`slash_command` / `register_repo_root`) と「**blocking 非対応・decision control なし**、失敗は debug ログのみ」まで記載
- **旧キーワード**: `イベント表には 2026-07-26 時点で未掲載`
- **波及ファイル**: `docs/hooks.md` (L149 のみ)
- 対応案: 注記を削除し、matcher 値と blocking 非対応を追記。matcher 表 (L153-166) にも `DirectoryAdded` 行を追加
- 出典: https://code.claude.com/docs/en/hooks

### 4. config-files.md — `sandbox.network.strictAllowlist` の「公式未反映」注記が不要になった

- 該当箇所: `docs/config-files.md` L221
- 現状: 「※ 公式 settings / sandboxing ページ**未反映**のため出典は CHANGELOG」
- 公式最新: settings ページの `sandbox` 表に掲載済み。加えて重要な制約が判明:
  - allowlist の定義 = `allowedDomains` + `WebFetch(domain:...)` allow rule (`allowManagedDomainsOnly` 時は managed のみ)
  - **sandbox 化コマンドのみ対象**。`WebFetch` 等の in-process tool は**対象外**
  - **user / managed / CLI `--settings` からのみ有効**。`.claude/settings.json` / `.claude/settings.local.json` に書いても**無効**
  - Default: `false`
- **旧キーワード**: `公式 settings / sandboxing ページ未反映のため出典は CHANGELOG`
- **波及ファイル**: `docs/config-files.md` (L221 のみ)
- 対応案: 注記を削除し、上記 4 点の制約を追記 (特に project スコープ無効は実務上の罠)
- 出典: https://code.claude.com/docs/en/settings

---

## B. 明確な事実誤り

### 5. best-practices.md — model alias プロバイダ別解決表が Opus 4.8 世代のまま (重要文書)

- 該当箇所: `docs/best-practices.md` L679-689
- 現状: 表が `Anthropic API = Opus 4.8` / `Claude Platform on AWS = Opus 4.7` / `Bedrock / Vertex / Foundry = Opus 4.6`。前置き文も「本書の他箇所で『`opus`→Opus 4.8』と書いているのは Anthropic API 前提」となっており、**前置き自体が現在は誤り**
- 公式最新 (verbatim):

  | Provider | `opus` | `sonnet` |
  |---|---|---|
  | Anthropic API | **Opus 5** | Sonnet 5 |
  | Claude Platform on AWS | **Opus 5** | Sonnet 4.6 |
  | Amazon Bedrock, Google Cloud's Agent Platform | **Opus 5** | Sonnet 4.5 |
  | **Microsoft Foundry** | **Opus 4.6** | Sonnet 4.5 |

  `default`: Max / Team Premium / Enterprise PAYG / Anthropic API / Claude Platform on AWS / Bedrock / Google Cloud = **Opus 5**、Pro / Team Standard / Enterprise seat = **Sonnet 5**
- 構造上の誤りも 2 点:
  - 公式は「Bedrock / Vertex / Foundry」を **1 行にまとめていない**。**Microsoft Foundry は独立行**で `opus` が Opus 4.6 のまま (= 唯一 Opus 5 に上がっていないプロバイダ)
  - 呼称は **Google Cloud's Agent Platform** (Vertex ではない)
- **旧キーワード**: `Bedrock / Vertex / Foundry | **Opus 4.6**` / `| Claude Platform on AWS | **Opus 4.7**` / `本書の他箇所で「\`opus\`→Opus 4.8」と書いているのは`
- **波及ファイル**: `docs/best-practices.md` (L679-689) / `docs/harness.md` (L308 に同じ旧プロバイダ依存記述あり) — 2 ファイル (grep 実施済み)
- 対応案: 表を Opus 5 世代に更新し、**旧世代の解決先は「v2.1.219 未満」「v2.1.207 未満」として履歴行に残す** (公式も同じ形で履歴を併記している)。§8.3 の Fable 5 節 (L659) の「`opus` エイリアスの解決先は依然 Opus 4.8」はリリース当時の記述としてラベル付けを明確化
- 出典: https://code.claude.com/docs/en/model-config
- ⚠️ **重要文書のため慎重な確認をお願いします**

### 6. sub-agents.md — 「subagent 変更後は再起動が必要」は現在誤り

- 該当箇所: `docs/sub-agents.md` L515 / L586 (トラブルシューティング表)
- 現状: 「セッション中にファイルを作成・変更した場合は、**再起動または `/agents` コマンドでリロードが必要**」/ 「ファイル作成後はセッション再起動が必要」
- 公式最新: 「Claude Code **watches** `~/.claude/agents/` and `.claude/agents/`. … Claude Code detects the change **within a few seconds** and the next delegation uses the updated definition, **with no restart needed**」。再起動が必要なのは **2 例外のみ** — ① そのスコープで初めて `agents` ディレクトリ自体を新規作成した場合 (watcher はセッション開始時に存在したディレクトリのみ対象) ② `--disable-slash-commands` 起動時
- **旧キーワード**: `再起動または \`/agents\` コマンドでリロードが必要` / `ファイル作成後はセッション再起動が必要`
- **波及ファイル**: `docs/sub-agents.md` (L515 / L586) — 2 箇所 (grep 実施済み。他ファイルの「再起動」ヒットは skills / plugins / desktop の別文脈で無関係)
- 対応案: 「watch により再起動不要 (数秒で反映)」に修正し、2 例外を明記
- 出典: https://code.claude.com/docs/en/sub-agents

### 7. sub-agents.md — Explore の model cap が「Opus 4.8」固定表記

- 該当箇所: `docs/sub-agents.md` L76
- 現状: 「メインが Opus 4.8 / Fable 5 / Sonnet 5 なら **Opus 4.8 に cap される**」
- 公式最新: 「inherits from the main conversation, **capped at Opus on the Claude API**, so Explore never runs on a more expensive model than the one you already chose」。バージョン固定の表記はなく、現在の cap 先は `opus` の解決先 = **Opus 5**。さらに「On any other provider, such as Amazon Bedrock, Google Cloud's Agent Platform, Microsoft Foundry, or Claude Platform on AWS, **Explore inherits the main conversation's model directly**」(= cap なし) も未記載
- **旧キーワード**: `Opus 4.8 に cap される`
- **波及ファイル**: `docs/sub-agents.md` (L76 のみ)
- 対応案: 「Claude API では Opus に cap (バージョン非固定)、他プロバイダは cap なしで直接継承」に修正
- 出典: https://code.claude.com/docs/en/sub-agents

### 8. sub-agents.md — frontmatter 表に存在しないフィールド `allowed-tools` が載っている

- 該当箇所: `docs/sub-agents.md` L212 付近の frontmatter 表
- 現状: `| allowed-tools | string/string[] | No | 許可なしで使えるツール |` の行がある
- 公式最新: subagent の frontmatter 公式リストは `name` / `description` / `tools` / `disallowedTools` / `model` / `permissionMode` / `maxTurns` / `skills` / `mcpServers` / `hooks` / `memory` / `background` / `effort` / `isolation` / `color` / `initialPrompt`。**`allowed-tools` は含まれない** (これは Skill 側のフィールド)。また `name` / `description` は「推奨」ではなく **Required = Yes**
- **旧キーワード**: `| \`allowed-tools\` | string/string[] | No | 許可なしで使えるツール |`
- **波及ファイル**: `docs/sub-agents.md` (L212 のみ)
- 対応案: 該当行を削除。`name` / `description` を必須に修正。`permissionMode` の受理値を `default` / `acceptEdits` / `auto` / `dontAsk` / `bypassPermissions` / `plan` / **`manual` (`default` のエイリアス、要 v2.1.200+)** に明記
- 出典: https://code.claude.com/docs/en/sub-agents

### 9. best-practices.md — auto mode 対応モデルの「Opus 5 は未確認」を解消できる

- 該当箇所: `docs/best-practices.md` L259 / L261
- 現状: 「※ **Opus 5 は公式の対応モデル一覧に明示列挙されていない** (2026-07-26 確認、未確認扱い)」/ 「3 プロバイダで対応するのは **Sonnet 5・Opus 4.7・Opus 4.8** の 3 モデル」
- 公式最新: permission-modes は依然 Opus 5 を**モデル名として列挙していない**が、条件が開区間表現である: Anthropic API / Claude Platform on AWS = 「Claude Opus **4.6 or later**, Sonnet 4.6 or later, or Fable 5」、Bedrock / Google Cloud / Foundry / Claude apps gateway = 「Sonnet 5, Opus **4.7 or later**, and Fable 5」。**"or later" に Opus 5 が構造的に含まれる**ため、「未確認」ではなく「対応 (開区間表現に包含)」と書ける
- **旧キーワード**: `Opus 5 は公式の対応モデル一覧に明示列挙されていない` / `**Sonnet 5・Opus 4.7・Opus 4.8** の 3 モデル`
- **波及ファイル**: `docs/best-practices.md` (L259 / L261) — 2 箇所 (grep 実施済み)
- 対応案: 「モデル名の明示列挙はないが `4.6 or later` / `4.7 or later` の開区間に含まれるため対応」と書き換え。L261 の「3 モデル」を「Sonnet 5・Opus 4.7 以降 (Opus 4.8 / Opus 5 を含む)」に修正
- 出典: https://code.claude.com/docs/en/permission-modes
- ⚠️ **重要文書のため慎重な確認をお願いします**

---

## C. 新機能・変更 (v2.1.221 由来 / すべて既存ファイルへの追記)

> v2.1.221 の内容は **公式 docs に一切反映されていない** (docs 全体で v2.1.220 / v2.1.221 の言及ゼロ)。出典は **CHANGELOG v2.1.221** と明記する。

### 10. [破壊的] `/fork` が独自の worktree を作成するようになった

- 追記先: `docs/slash-commands.md` (L156 の `/fork` バージョン別挙動表) / `docs/session-history.md` (L98-109 の 3 経路比較) / `docs/sub-agents.md` (L90 / L120)
- 要点: 「Changed sessions forked with `/fork` to create a **new worktree of their own** instead of working in the original session's checkout」。**v2.1.212 の再定義 (会話を別 background セッションへ複製) をさらに上書き**する変更で、fork 先が元セッションの checkout を共有しなくなった。ディスク上のセッション同一性を扱う session-history.md の記述に直接影響する
- **旧キーワード**: (追記のため無し。ただし「元セッションの checkout で作業」を前提にした記述があれば要修正)
- **波及ファイル**: `docs/slash-commands.md` / `docs/session-history.md` / `docs/sub-agents.md`
- 出典: CHANGELOG v2.1.221

### 11. [破壊的] background session の完了時挙動が変更された

- 追記先: `docs/sub-agents.md` L133 (v2.1.198 の「自動 commit/push/PR」記述の直後)
- 要点: 「Changed background sessions to **commit and push to preserve work**, open a **draft PR only when the task calls for one**, **follow your CLAUDE.md git instructions**, and always end by **reporting where the work lives**」。v2.1.198 の「自動で commit + push + draft PR まで走らせる」から **draft PR が条件付きに後退**し、代わりに **CLAUDE.md の git 指示に従う**ようになった。本リポの CLAUDE.md / グローバル設定に git 運用ルールを書いている BOSS の運用に直接効く
- **旧キーワード**: `自動で commit + push + draft PR まで走らせる`
- **波及ファイル**: `docs/sub-agents.md` (L133 のみ。grep 実施済み)
- 出典: CHANGELOG v2.1.221

### 12. sandbox credential files に `mode: "mask"` が追加された

- 追記先: `docs/config-files.md` L221 (`sandbox.credentials` の説明)
- 要点: Linux / WSL で**ファイルもマスク可能**になった。sandbox 内はセンチネルのコピー (ファイル全体、または `extract` 正規表現が捕捉したスパンのみ) を読み、egress 時に sandbox proxy が実値へ置換する。**macOS ではファイルマスクは `deny` にフォールバック**。従来はファイルは `deny` のみだった
- 出典: CHANGELOG v2.1.221

### 13. plugin の即時有効化 + `/plugin install` のカタログ更新 + `skills: "."`

- 追記先: `docs/plugins.md` (L311 / L384 / L439 / L665 の「`/reload-plugins` で反映」記述)
- 要点: ① `/plugin` からインストールした plugin は**安全なら `/reload-plugins` なしで即有効** ② `/plugin install` は stale な marketplace カタログを更新して再試行してから「not found」を返す ③ plugin が `skills` パスに **`"."` を受理**し、root-level `SKILL.md` の検証エラーも plugin root を案内するようになった
- **旧キーワード**: `Plugin のインストール・有効化・無効化後は \`/reload-plugins\` で即時反映できる`
- **波及ファイル**: `docs/plugins.md` (L311 / L384 / L439 / L665)
- 出典: CHANGELOG v2.1.221

### 14. `/status` がセッション種別を表示するようになった

- 追記先: `docs/slash-commands.md` L258 (`/status` の説明)
- 要点: `interactive` / background の `attached` / `unattended` を表示。併せて公式 commands の現行説明は「Open the Settings interface on the Status tab, showing version, model, account, and connectivity. **Works while Claude is responding**」で、ローカルの既存説明は正しい (「応答中も実行可」の追記のみ必要)
- 出典: CHANGELOG v2.1.221 / https://code.claude.com/docs/en/commands

### 15. `claude-api` skill に `prompt-audit` サブコマンドが追加された

- 追記先: `docs/slash-commands.md` (`/claude-api` の行)
- 要点: プロンプトとツール説明を「**旧世代モデル向けに書かれたパターン**」の観点で監査するサブコマンド。Opus 5 世代への移行支援。公式 docs の `/claude-api [migrate|managed-agents-onboard]` は未更新
- 出典: CHANGELOG v2.1.221

### 16. セキュリティ修正 2 件 (権限チェックのバイパス)

- 追記先: `docs/config-files.md` (permission のセキュリティ修正履歴。L160-196 の permission ルール節)
- 要点: ① **zsh の `[[ ]]` 正規表現条件式**に隠したコマンドが Bash tool の権限チェックを回避して実行できた → 該当コマンドは権限プロンプト対象に ② **Windows の PowerShell 権限チェックが引用符を含むパスを誤処理** → 該当パスは承認プロンプト対象に。前回反映した v2.1.214 のセキュリティ修正群 (PowerShell 5.1 / fd リダイレクト / file コマンド) と同系列
- 出典: CHANGELOG v2.1.221

### 17. Opus 5 の thinking / effort 周辺のバグ修正

- 追記先: `docs/model-comparison.md` §3.3 (Opus 5 の thinking 節) / §6.5
- 要点: ① **thinking off で開始したセッションで、以降 thinking トグルが無効だった**バグを修正 ② **`WebSearch` が effort `xhigh` / `max` かつ thinking 無効時に 400 で失敗**するバグを修正。どちらも既知の「Opus 5 は thinking 既定 ON、無効化は effort high 以下のみ」制約 (既知 #2 / #7) の周辺挙動で、**v2.1.220 以前では回避策が必要だった**ことを意味する
- 出典: CHANGELOG v2.1.221

### 18. その他の挙動変更・修正 (まとめて 1 項目)

- 追記先: 該当 doc の各節 (下記)
- 要点:
  - `CLAUDE_CODE_RESUME_INTERRUPTED_TURN=0` が効かない不具合を修正 (falsy 値を尊重) → `docs/config-files.md` env 節
  - `-p` (print mode) で `--mcp-config` の MCP サーバが初回ターン前に接続されず、**モデルが tool call をリテラルテキストとして出力**する不具合を修正 → `docs/mcp-setup.md`
  - MCP: スリープ復帰時に 2 プロセスが同一 MCP connector / WIF トークンを同時 refresh する race を修正 (再認証を強制されていた) → `docs/mcp-setup.md`。**BOSS の MCP 再認証回避運用に関係する**
  - auto mode: 並列 tool call の権限チェックを cache 効率化 / 判定保留中のモード切替で stale な結果を適用せず確実にプロンプト / 会話 prefix の cache 再利用で prompt-cache コスト削減 → `docs/config-files.md` auto mode 節
  - plugin / org 配布の skill が `/help` 等ターミナル組込名と同名だと**非対話セッションで起動不能**だった不具合を修正 → `docs/skills.md` / `docs/plugins.md`
  - Gateway の `model` フィールドが非文字列値を **400 で拒否** (gateway クライアントに対する破壊的変更) → `docs/config-files.md`
  - Stats パネルが cache トークンを合計に含め、input / output / cache read / cache write の内訳を表示 → `docs/slash-commands.md` (`/usage` 系)
  - `/ultrareview` のエラーメッセージ改善 (base と履歴を共有しない repo、ブランチなし checkout の事前拒否) → `docs/slash-commands.md`
  - Google Vertex AI で **tool search が Claude 4.5 世代以降に限り再有効化** → `docs/mcp-setup.md` (項目 22 と併せて反映)
  - emoji 補完が `:thumbsup:` `:thumbsdown:` `:love:` 等の別名 shortcode を受理 → `docs/config-files.md` (`emojiCompletionEnabled`)
- 出典: CHANGELOG v2.1.221
- 備考: **VS Code の Focus view** (`Ctrl+Alt+F`、ツール実行ログをターン単位の展開可能サマリに畳む) は v2.1.221 の新機能だが、本リポに IDE 拡張の解説 doc が無いため**記載先がない**。項目 36 (見送り) 側に記録する

---

## D. 公式にあるが未記載の欠落

### 19. hooks.md — ハンドラ仕様・既定値の欠落 (7 点)

- 該当箇所: `docs/hooks.md` L80 (`timeout`) / L88 (`async`) / L96-102 (環境変数) / L140 (`WorktreeCreate`) / L153-166 (matcher 表)
- 現状 / 公式最新:

  | 項目 | 現状 | 公式最新 |
  |---|---|---|
  | `asyncRewake` | **キー欠落** (`async` のみ記載) | `true` でバックグラウンド実行し、**exit code 2 で rewake** (`async` を含意)。stderr (空なら stdout) が system reminder として Claude に渡る |
  | timeout 既定値 | `command`/`http`/`mcp_tool` = 600 のみ | + **`prompt` = 30 / `agent` = 60**。例外: `UserPromptSubmit` = 30 / `MessageDisplay` = 10 / `SessionEnd` は**共有予算 1.5 秒** (per-hook timeout で最大 60 秒まで引き上げ) |
  | 出力サイズ上限 | 未記載 | `additionalContext` / `systemMessage` / stdout は **10,000 文字でキャップ**。超過分はファイルに退避しパスとプレビューを渡す |
  | 重複排除 | 未記載 | 同一ハンドラは並列実行時に自動 dedup (command は command 文字列 + args、http は URL で判定) |
  | hook が読める環境変数 | パスプレースホルダ 3 種のみ | + **`$CLAUDE_CODE_REMOTE`** / **`$CLAUDE_CODE_BRIDGE_SESSION_ID`** / **`$CLAUDE_EFFORT`** / **`$CLAUDE_PLUGIN_OPTION_<KEY>`**。加えて **`OTEL_*` exporter 変数は全 hook subprocess から除去される** |
  | `PermissionRequest` 出力形 | 未記載 | `hookSpecificOutput.decision = { behavior: "allow"\|"deny", updatedInput: {...} }` (`permissionDecision` ではない) |
  | `WorktreeCreate` | 「ブロック可」のみ | **非ゼロ exit code すべて**が作成を中止 (exit 2 限定ではない) |

- matcher 表の欠落: `DirectoryAdded` / `StopFailure` (`rate_limit` / `overloaded` / `authentication_failed` / `billing_error` / `invalid_request` / `model_not_found` / `server_error` / `max_output_tokens` / `unknown`) / `ConfigChange` (`user_settings` / `project_settings` / `local_settings` / `policy_settings` / `skills`) の 3 行が無い。`InstructionsLoaded` は `include` のみで、公式は `session_start` / `nested_traversal` / `path_glob_match` / `include` / `compact` の 5 値
- `SessionStart` のトリガー欄 (L112) に **`fork`** が抜けている (matcher 表 L158 には有り) — ファイル内不整合
- **旧キーワード**: `` `command`/`http`/`mcp_tool` の既定 600 ``
- **波及ファイル**: `docs/hooks.md`
- 出典: https://code.claude.com/docs/en/hooks

### 20. mcp-setup.md — MCP tool search 節が丸ごと欠落 + timeout の数値不足

- 該当箇所: `docs/mcp-setup.md` (grep で `tool search` / `alwaysLoad` / `MAX_MCP_OUTPUT_TOKENS` / `ENABLE_TOOL_SEARCH` がすべて **0 件**)
- 公式最新:

  | 項目 | 公式の記述 |
  |---|---|
  | tool search が既定 ON | MCP tools は upfront ロードではなく **deferred**。セッション開始時にロードされるのは**ツール名と server instructions のみ** |
  | `ENABLE_TOOL_SEARCH` | (unset) = 全 defer / `true` = 強制 defer / **`auto` = コンテキストの 10% に収まれば upfront** / `false` = 無効 |
  | 無効化される環境 | Google Cloud's Agent Platform、非 first-party `ANTHROPIC_BASE_URL`、Microsoft Foundry (Azure ホスト) はサーバー側 reject。**Google Vertex AI は v2.1.221 で Claude 4.5 世代以降に限り再有効化** |
  | `alwaysLoad: true` | `.mcp.json` の per-server キー。該当サーバーのみ upfront ロードし、**接続まで起動をブロック** (5 秒 connect timeout でキャップ) |
  | `MAX_MCP_OUTPUT_TOKENS` | 出力が **10,000 トークン超で警告**、**既定 25,000 トークンで制限**。警告閾値は固定。`anthropic/maxResultSizeChars` 宣言ツールは text content でそちらを優先 |
  | idle timeout の既定値 | ローカルは「5 分」のみ。公式は **HTTP / SSE / WebSocket / claude.ai connector = 5 分、stdio = 30 分** (stdio は v2.1.203 で対象化)。`CLAUDE_CODE_MCP_TOOL_IDLE_TIMEOUT` (ms) で変更、`0` で無効化。**per-server `timeout` (≥1000) は idle timeout の下限として機能** |
  | `enabledMcpServers` | default-off の built-in (`computer-use` 等) を opt-in するリスト |

- **旧キーワード**: `5 分 idle timeout`
- **波及ファイル**: `docs/mcp-setup.md`
- 備考: **BOSS は 18 MCP を並行稼働させており、tool search が既定 ON = 「MCP を繋ぎ過ぎても実消費はほぼ 0」という運用判断の根拠**にあたる。メモリ (`reference_claude_md_context_optimization`) の内容と整合する公式記述なので、doc 化の価値が高い
- 出典: https://code.claude.com/docs/en/mcp

### 21. memory.md — import / paths の上限値が未記載

- 該当箇所: `docs/memory.md` (grep で `hop` / `1,000` / `4 MiB` がすべて 0 件)
- 公式最新:
  - `@path` import は「recursively import other files, with a **maximum depth of four hops**」
  - import parse は **Markdown code span / fenced code block をスキップ** (`` `@README` `` はリテラル、`@README` は import)
  - `paths` の brace 展開は「a rule's whole `paths` list shares one budget of **1,000 expanded patterns and 4 MiB**」。超過分は**未展開のまま使われリテラルの `{}` として何にもマッチしない** (v2.1.217 未満は起動時に stall / crash)
  - glob の `[` は bracket expression 扱い。`photos [2024/**` は不正で何にもマッチしない (v2.1.207 未満は Read tool が全ファイルで失敗)。リテラルは `photos \[2024/**`
  - **プロジェクトスコープ memory から作業ディレクトリ外を import すると初回に承認ダイアログ** (拒否すると以後 import は無効のまま、ダイアログも再表示されない)。user スコープの import はダイアログなし
- **波及ファイル**: `docs/memory.md`
- 備考: 本リポの CLAUDE.md は `@import` を 5 本使っており、**4 hops 制限は実運用に直接関わる**
- 出典: https://code.claude.com/docs/en/memory

### 22. model-comparison.md / slash-commands.md — fast mode の課金モデルが未記載

- 該当箇所: `docs/model-comparison.md` §6.5 / `docs/slash-commands.md` L352-373
- 公式最新 (verbatim 重要):
  - 「For Claude Code users on subscription plans (Pro/Max/Team/Enterprise), fast mode is available **via usage credits only and not included in the subscription rate limits**」
  - 「**Fast mode usage draws directly from usage credits, even if you have remaining usage on your plan.** This means fast mode tokens do not count against your plan's included usage and are charged at the fast mode rate **from the first token**」
  - 初回有効化時に**会話コンテキスト全体の uncached input を fast 料金で払う**。深い会話で ON にすると高い (1 会話につき 1 回のみ)
  - 「All supported Opus models **share one fast mode rate limit pool**」
  - Team / Enterprise は**既定で無効**、Owner が明示的に有効化する必要あり
  - 未記載の env: **`CLAUDE_CODE_DISABLE_FAST_MODE`** / **`CLAUDE_CODE_SKIP_FAST_MODE_NETWORK_ERRORS`** / **`CLAUDE_CODE_SKIP_FAST_MODE_ORG_CHECK`** (LLM gateway 配下で `/fast` が「network connectivity issues」になる既知問題の対処)
  - 未記載の settings: **`fastMode`** / **`fastModePerSessionOptIn`**
- **波及ファイル**: `docs/model-comparison.md` / `docs/slash-commands.md`
- 備考: `model-comparison.md` は**プラン消費特性を主題にした文書**なので、「fast mode はプラン枠を消費せず credits から引かれる」の欠落は本文書の中核に関わる
- 出典: https://code.claude.com/docs/en/fast-mode

### 23. config-files.md — settings キーの体系的な欠落

- 該当箇所: `docs/config-files.md` L221 / L224 周辺
- 公式 settings ページに掲載されていて grep 0 件だった主要キー:
  - **`autoMode.environment` / `autoMode.allow` / `autoMode.soft_deny` / `autoMode.hard_deny`** (`environment` に `"$defaults"` を含めて built-in を継承する仕様) — ローカルは `autoMode.classifyAllShell` のみ
  - **sandbox 系**: `sandbox.enabled` / `network.allowDomains` / `denyDomains` / `allowUnixSockets` / `credentials.files` / `credentials.envVars` / `autoAllowBashIfSandboxed` / `network.tlsTerminate` / `filesystem.allowRead` `denyRead` の「より狭い方が勝つ」解決規則
  - **`permissions.additionalDirectories`** / **`allowedHttpHookUrls`** / **`disableAgentView`** / **`disableSkillShellExecution`** / **`fileCheckpointingEnabled`** / **`showClearContextOnPlanAccept`** / **`includeGitInstructions`** / `apiKeyHelper` / `fileSuggestion` / `deniedMcpServers` / `allowAllClaudeAiMcps` / `strictKnownMarketplaces` / `editorMode` 他
  - 仕様上の注意: `sandbox.filesystem.disabled` は user / managed / `--settings` のみ設定可 (project 不可) かつ managed が `sandbox.filesystem` または `credentials.files` を設定している場合は **managed のみ**設定可
  - 公式は「**Permission rules merge across scopes; other settings follow priority order**」と明記しているが、ローカル L103-113 の優先順位節に**permission ルールのマージ挙動**の記載がない
- **波及ファイル**: `docs/config-files.md`
- 対応案: 全キーを網羅するのではなく、**BOSS の運用に効く群 (autoMode.* / sandbox 系 / permissions.additionalDirectories / permission ルールのマージ挙動) を優先追記**し、残りは公式リンクに委ねる (progressive disclosure の方針に沿う)
- 出典: https://code.claude.com/docs/en/settings

### 24. config-files.md — permission ルール節の追記 4 点

- 該当箇所: `docs/config-files.md` L160-196
- 公式最新:

  | 項目 | 公式の記述 |
  |---|---|
  | `Read` deny の波及 | 「A **`Read` deny rule also blocks the Edit tool** on the same path, including creating a new file there. **Write and NotebookEdit aren't covered**, so add an `Edit` deny rule for paths no tool may change」(要 v2.1.208+) |
  | never-matched の対象 | `Write` / `NotebookEdit` / `Glob` / **legacy `MultiEdit`**。ただし **`--allowedTools` で渡した `Glob` ルールだけは起動時警告が出ない** |
  | 起動時警告の文面 | `Permission deny rule (.claude/settings.json): Write(docs/**) is not matched by file permission checks — only Edit(path) rules are. Use Edit(docs/**) instead` |
  | canonical tool 名 | transcript のラベルと canonical 名が異なる例を公式が明示: 「the tool labeled **`Stop Task`** in the transcript has the canonical name **`TaskStop`**」。ルール・hook matcher は canonical 名のみマッチ |

- **波及ファイル**: `docs/config-files.md`
- 備考: 本リポの `.claude/settings.json` は `permissions.deny` を使っており、**`Read` deny が `Edit` も塞ぐ / `Write(...)` は never matched** は設定の正しさに直結する
- 出典: https://code.claude.com/docs/en/permissions

### 25. slash-commands.md — 公式コマンド表にあってローカルに無いコマンド 7 件

- 該当箇所: `docs/slash-commands.md` のコマンド表
- grep 0 件: **`/usage-credits`** (limit 到達時に usage credits を設定 or admin へ申請。旧名 `/extra-usage`。Team/Enterprise で billing 権限なしのメンバーは CLI から admin へ申請、v2.1.211 以降は確認ダイアログあり。ブラウザが開けない場合は URL を出力 = v2.1.205+) / **`/chrome`** (Claude in Chrome 設定) / **`/copy [N]`** (直近応答をクリップボードへ。`/copy 2` で 2 番目) / **`/desktop`** (現セッションを Desktop アプリで継続。macOS or x64 Windows + サブスク必須。alias `/app`) / **`/export [filename]`** (会話をプレーンテキスト出力) / **`/mobile`** (モバイルアプリ DL の QR 表示。alias `/ios` `/android`) / **`/upgrade`** (Enterprise プランでは非表示)
- 関連 env: **`DISABLE_EXTRA_USAGE_COMMAND`** も未記載
- **波及ファイル**: `docs/slash-commands.md`
- 備考: `/workshop` は**公式 commands ページに依然掲載なし**のため見送り継続 (項目 36)
- 出典: https://code.claude.com/docs/en/commands

### 26. sub-agents.md — 未記載の公式仕様 4 点

- 該当箇所: `docs/sub-agents.md`
- 公式最新:

  | 項目 | 公式の記述 |
  |---|---|
  | `Agent(agent_type)` allowlist | `claude --agent` でメインスレッド起動した agent の `tools` に `Agent(worker) Agent(researcher)` と書くと **spawn できる subagent 型を allowlist 制限**できる。subagent 定義内では括弧内の型リストは**無視される** |
  | background subagent の具体的ツールセット | `Agent` / `ExitPlanMode` を除き、background subagent が保持する built-in は **`Read, Grep, Glob, Bash, PowerShell, Edit, Write, NotebookEdit, WebFetch, WebSearch, TodoWrite, Skill, ToolSearch, EnterWorktree, ExitWorktree, Monitor, TaskStop, SendMessage, Artifact` のみ**。MCP tool は全保持。**同じ定義が fg / bg で違うツール集合に解決される** |
  | per-session cap のリセット | 「Run **`/clear`** to reset the count」。ただし workflow など clear を跨いで生き残る作業があれば count は繰り越し |
  | `tools` / `disallowedTools` の解決順 | 両方設定時は **`disallowedTools` を先に適用し、残りのプールに対して `tools` を解決**。両方に載ったツールは除去される。MCP は `mcp__<server>` / `mcp__<server>__*` のサーバー単位パターン可、`disallowedTools` では `mcp__*` で全 MCP 除去 |

- **波及ファイル**: `docs/sub-agents.md`
- 出典: https://code.claude.com/docs/en/sub-agents

### 27. plugins.md — skills ディレクトリ型 plugin と単一 SKILL.md 型が未記載

- 該当箇所: `docs/plugins.md` (grep で `skills-dir` が 0 件)
- 公式最新:
  - **`claude plugin init my-tool`** は `~/.claude/skills/my-tool/` に `.claude-plugin/plugin.json` + starter `SKILL.md` を生成し、次セッションで **`my-tool@skills-dir`** として **marketplace も install も不要で自動ロード**される (`--plugin-dir` を毎回渡す必要がない開発フロー)
  - **1 スキルだけの plugin は `skills/` を作らず plugin ルートに直接 `SKILL.md` を置ける** (frontmatter の `name` が invocation 名になる)
  - `claude-plugins-official` は**初回の対話起動時に自動登録**される。非対話起動が先だった / marketplace policy でブロックされた場合は手動 `claude plugin marketplace add anthropics/claude-plugins-official`
  - `/reload-plugins` のサマリの skills 件数は **`commands/` ディレクトリのみを数える**ため、`skills/` を編集しても `0 skills` と表示され得る (既知の紛らわしい挙動)
  - plugin ルートの `settings.json` は **`plugin.json` 内の `settings` より優先**、未知キーは silent ignore
- **波及ファイル**: `docs/plugins.md`
- 備考: BOSS は avengers リポで 29 スキル + JARVIS Plugin を symlink 運用しており、**`@skills-dir` 自動ロードは運用簡素化の選択肢**になる
- 出典: https://code.claude.com/docs/en/plugins

### 28. claude-desktop.md — 公式 desktop ページとの乖離 + 出典の要再確認

- 該当箇所: `docs/claude-desktop.md` (全 92 行)
- 公式ページにあってローカルに無い主要事項:
  - Desktop は **Chat / Cowork / Code の 3 タブ**構成 (Code タブが Claude Code 相当)。**Linux beta** (apt / .deb) 提供
  - **`sshHostAllowlist`** — managed settings のみで読まれ、**Desktop アプリのみが honor** (CLI / IDE 拡張は読まない。Bash 経由の `ssh` は制限しない)
  - `disableBrowserExternalNavigation` は **JSON boolean の `true` のみ有効** (文字列 `"true"` は無視)
  - **Desktop の `/config` は `key=value` を無視する** (`/config theme=dark` が効かない) → `docs/slash-commands.md` L257 の `/config key=value` は **CLI 前提**である旨の注記が必要
  - `MAX_THINKING_TOKENS=0` の挙動がモデル世代で分岐 (Fable 5 は無効、adaptive reasoning モデルは 0 以外を無視、Opus 4.6 / Sonnet 4.6 は `CLAUDE_CODE_DISABLE_ADAPTIVE_THINKING=1` で固定 budget 化可能)
  - `.worktreeinclude` で gitignore されたファイル (`.env` 等) を worktree にコピー
- ⚠️ **出典の再確認が必要**: ローカル L24-29 の「hosted セッションは mTLS / CA バンドル / OAuth scope を警告付きで無視 (v2.1.212)」は CHANGELOG 由来のままで、**現行の公式 desktop ページ本文では確認できなかった** (同ページは mTLS / 独自 CA / proxy を `/docs/en/network-config` へのリンクで扱う形に変わっている)。誤りとは断定できないため、**「出典: CHANGELOG v2.1.212」と明示する**対応を推奨
- **波及ファイル**: `docs/claude-desktop.md` / `docs/slash-commands.md` (L257)
- 出典: https://code.claude.com/docs/en/desktop

### 29. skills.md — `${CLAUDE_SKILL_DIR}` の plugin 時の解決先

- 該当箇所: `docs/skills.md` L149
- 現状: 「スキルの `SKILL.md` が存在するディレクトリパス」
- 公式最新: 「For plugin skills, this is the skill's **subdirectory within the plugin, not the plugin root**」と明示
- **波及ファイル**: `docs/skills.md` (L149 のみ)
- 備考: skills.md はこの 1 点を除き**公式と完全一致**を確認 (frontmatter 18 フィールド / 文字置換 6 種 / `background` 既定 `true` / 1,536 文字キャップ / `EndConversation` 除去不可)
- 出典: https://code.claude.com/docs/en/skills

---

## E. モデル / ブログ由来

### 30. model-comparison.md — Opus 5 の tokenizer 世代が確定した (「未確認」を解消)

- 該当箇所: `docs/model-comparison.md` (Opus 5 節の「tokenizer は公式に明示なし」記述)
- 現状: 前回「公式に明示なし」として未確認扱い (deferred_items #18)
- 公式最新 (verbatim): 「**Claude Opus 4.7 introduced a new tokenizer, which later Opus models, including Claude Opus 5, also use.** … it may use roughly **1x to 1.35x** as many tokens when processing text compared to models before Claude Opus 4.7 (up to ~35% more, varying by content)」。Opus 4.7 / 4.8 / Opus 5 / Fable 5 / Mythos 5 が**同一 tokenizer**。`max_tokens` と compaction トリガの再調整、クライアント側トークン推定の再テストが推奨
- **旧キーワード**: `tokenizer` + `未確認` の組み合わせ箇所
- **波及ファイル**: `docs/model-comparison.md` (要 grep 再確認) / `docs/best-practices.md` L635 (Opus 4.8 の tokenizer 未確認注記)
- 出典: https://platform.claude.com/docs/en/about-claude/models/migration-guide

### 31. model-comparison.md — Sonnet 5 の introductory pricing 終了日を明示

- 要点: overview 脚注 4 に「$2 / $10 per MTok applies to Claude Sonnet 5 **through August 31, 2026**」。**あと 1 ヶ月未満で標準 $3 / $15 に戻る**
- **波及ファイル**: `docs/model-comparison.md`
- 対応案: 「2026-08-31 まで」を強調し、以降 $3/$15 に戻る旨を明記 (BOSS のコスト見積もりに直接効く)
- 出典: https://platform.claude.com/docs/en/about-claude/models/overview

### 32. model-comparison.md — Opus 4.1 retirement (2026-08-05) と公式内の代替表記の揺れ

- 要点: `claude-opus-4-1-20250805` は 08-04 時点で **Deprecated のまま・retirement 未実行**、tentative date は **2026-08-05 (明日)** で据え置き。ただし **deprecations ページの履歴表は代替を `claude-opus-4-8` とし、overview の Warning は「Opus 5 へ移行せよ」**と記載しており、公式内で代替モデルの表記が食い違っている (実務上は Opus 5 推奨で問題なし)
- 併せて: overview の "Legacy models" に Opus 4.8 / 4.7 / 4.6 / 4.5 / Sonnet 4.6 / 4.5 が入っている一方、deprecations ページの表では同モデルが **Active**。**ライフサイクル用語と表示区分が別軸**である点は doc 化時に混同注意
- **波及ファイル**: `docs/model-comparison.md`
- 出典: https://platform.claude.com/docs/en/about-claude/model-deprecations

### 33. [新規トピック] model-comparison.md — claude.ai 上の Opus 5 が分類器フラグ時に Opus 4.8 へ自動フォールバックする

- 要点: claude.ai (Claude アプリ) では、分類器がメッセージをリスク判定した際に **Opus 5 → Opus 4.8 へ自動フォールバック**し、**以降その会話は下位モデルのまま固定**される。Settings > Capabilities の「Switch models when a message is flagged」で無効化可
- 位置づけ: 既知 #10 (ClaudeCode 側の classifier fallback カテゴリ別化) とは**別レイヤの挙動** (Claude アプリ側)。model-comparison.md の fallback 節に追記が妥当
- **波及ファイル**: `docs/model-comparison.md`
- 出典: https://support.claude.com/en/articles/16049681-why-claude-switched-models-in-your-conversation-with-opus-5

### 34. [新規ブログ] best-practices.md / harness.md — 07-21 「How Anthropic secures its AI-native SDLC」

- 前回の取りこぼし (07-21 公開、既知リストに無し)。Claude Code カテゴリの記事
- 実務要点:
  - **セキュアコーディング指針を CLAUDE.md に埋める** — 「コードが生成された瞬間からベストプラクティスに従う」。脆弱性が見つかったら該当ファイルを更新して再発を防ぐ**閉ループ**
  - **`/security-review` を生成時に組み込む** — 「攻撃者が制御可能な入力の侵入点を探し、疑わしいリンクをスキャンし、検出結果を検証する」
  - **egress allowlist 付きリモート VM** で作業し、prompt-injection ペイロードに遭遇しても**exfiltration path を持たせない**
  - **Principle of Least Agency** — 各 agent に職務上最小の権限のみ (インシデント対応 agent はドキュメント作成 / Slack 投稿 / ログ参照のみ、修正のデプロイは不可)
  - **広範な単一レビュアーではなく、狭いスコープのレビュー agent を複数** — 「they do not share biases and blindspots」
  - **新レビュアーは shadow mode から** — 人間承認前提でコメントを投稿させ、信頼を獲得してから昇格。チームは意図的に悪性変更を挿入して信頼性を試験する
- **波及ファイル**: `docs/best-practices.md` (セキュリティ節) / `docs/harness.md` (Evaluator 設計節)
- 備考: **harness.md の Planner / Generator / Evaluator 構成に対する公式側の補強材料**。「Evaluator を 1 体の万能レビュアーにしない」根拠として引用価値が高い
- 出典: https://claude.com/blog/how-anthropic-secures-its-ai-native-software-development-lifecycle

### 35. [新規ブログ] harness.md / mcp-setup.md — 07-28 「Bringing MCP 2026-07-28 to Claude」

- 要点: MCP 仕様の 2026-07-28 版で、① コアが **stateful → ステートレスな request/response** へ ② MCP Apps / Tasks が **バージョン付き extensions** に分離 ③ 認可が **OAuth 2.0 / OIDC 準拠** (Entra / Okta 等) へ
- **未確認**: Claude 製品への展開は「rolling out soon」で、**ClaudeCode 側の対応状況は本記事に明記なし**。自作 MCP サーバを持つ場合は spec / SDK の更新確認が必要
- **波及ファイル**: `docs/mcp-setup.md` / `docs/harness.md`
- 対応案: 「仕様側の変更で ClaudeCode 実装の追従状況は未確認」と明示して追記する (憶測で「対応済み」と書かない)
- 出典: https://claude.com/blog/bringing-mcp-2026-07-28-to-claude

---

## F. 見送り継続 (公式で裏が取れず)

### 36. 記載先がない / 公式未掲載のため今回も見送る項目

- **`/workshop`**: 公式 commands ページ (raw markdown 全文 179KB を grep) と `llms.txt` インデックスの**両方にヒットなし**。説明を憶測で書かないため見送り継続
- **`claude rc`**: cli-reference のサブコマンド表に存在しない。見送り継続
  - ※ 前回の裏取り待ちのうち **`claude daemon` / `claude attach` / `claude rm` / `--max-budget-usd` (print mode 限定) / `--betas` (API key ユーザー限定) は公式掲載を確認済み**。これらは項目 25 と併せて反映可
- **VS Code Focus view** (v2.1.221): 本リポに IDE 拡張の解説 doc が無いため記載先がない。将来 `docs/ide-extensions.md` を作る際の候補として state に記録する
- **Opus 5 のプラン別 weekly limit 消費速度**: 依然として公式に数値の記述なし (定性的記述のみ)。断定を避け未確認扱いを維持
- **Dreams (research preview) が Opus 5 対応** (platform release notes 08-01): managed agents の話で本リポ 3 ドキュメントへの反映優先度は低い。見送り

---

## 反映優先順位 (JARVIS の推奨)

| 優先 | 項目 | 理由 |
|---|---|---|
| **最優先** | 1-4 | 誤った「公式未反映」警告が**読者を誤らせている**。公式が追従した以上、注記の存在自体が誤情報 |
| **最優先** | 5-9 | 明確な事実誤り。特に 5 (alias 表) と 6 (再起動不要) は実務判断を誤らせる |
| **高** | 10-11 | v2.1.221 の破壊的変更。`/fork` の worktree 化と background session の git 挙動は BOSS の運用に直接効く |
| **高** | 19-22 | hooks の欠落キー・MCP tool search・fast mode 課金・memory 上限。いずれも実運用の意思決定に直結 |
| **中** | 12-18, 23-27 | v2.1.221 の残り + settings / permission / コマンド / plugin の穴埋め |
| **中** | 30-34 | モデル仕様の確定 (tokenizer)、価格の期限、セキュリティ記事 |
| **低** | 28-29, 35 | claude-desktop の乖離 (出典再確認が必要)、skills の軽微差分、MCP spec (ClaudeCode 対応未確認) |

---

## 承認のお願い

上記のうち、反映してよい項目を番号でお知らせください。
(例: 「全部」「1-9 と 19-22 のみ」「28 は却下、他は OK」など)

**私の推奨は「1-35 を全部反映」**です。理由は 3 点:

1. **A 群 (1-4) と B 群 (5-9) は現在の記述が誤りであり、放置すると誤情報を配信し続ける**
2. **C 群 (10-18) は CHANGELOG v2.1.221 が唯一の一次情報**で、次回同期まで待つと公式 docs 追従を待つ形になり鮮度が落ちる
3. **D 群 (19-29) は「公式にあるのに書いていない」欠落**で、量は多いが判断の余地が少ない (公式記述の転記 + 日本語化)

ただし **項目 23 (settings キーの体系的欠落)** は公式キーが 25 個以上あり、全網羅すると config-files.md が肥大化します。**BOSS の運用に効く群 (autoMode.* / sandbox 系 / permissions.additionalDirectories / permission ルールのマージ挙動) に絞り、残りは公式リンクに委ねる**方針を提案します。この絞り込みで良いかもご判断ください。
